swagger: "2.0"
info:
  version: "0.0.1"
  title: TelPro - FileMaker App (Hive)
# during dev, should point to your local machine
host: 52.34.207.5:5058
#host: localhost:5000
# basePath prefixes all resource paths 
basePath: /
# 
schemes:
  # tip: remove http to make production-grade
  - http
  - https
# format of bodies a client can send (Content-Type)
consumes:
  - application/json
# format of the responses to the client (Accepts)
produces:
  - application/json
paths:
  /api/azureSignIn:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth Module']
      description: User authentication with email and password
      summary: User authentication with email and password
      # used as the method name of the controller
      operationId: azureAuth
      parameters:
        - name: body
          in: body
          description: User authentication with email and password
          schema:
           $ref: '#/definitions/userAuth'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/signIn:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth Module']
      description: User Sign In
      summary: User Sign In
      # used as the method name of the controller
      operationId: signIn
      parameters:
        - name: body
          in: body
          description: User Sign In
          schema:
           $ref: '#/definitions/signIn'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getAllUsers:
    # binds a127 app logic to a route
    x-swagger-router-controller: users
    post:
      tags: ['User Management']
      description: Get All Users
      summary: Get All Users
      # used as the method name of the controller
      operationId: getAllUsers
      parameters:
        - name: body
          in: body
          description: Get All Users
          schema:
           $ref: '#/definitions/getAllUsers'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getUserById:
    # binds a127 app logic to a route
    x-swagger-router-controller: users
    post:
      tags: ['User Management']
      description: Get User Details
      summary: Get User Details
      # used as the method name of the controller
      operationId: getUserById
      parameters:
        - name: body
          in: body
          description: Get User Details
          schema:
           $ref: '#/definitions/getUserById'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getTasksList:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard_ctrl
    post:
      tags: ['Dashboard Module']
      description: Get Company Tasks List
      summary: Get Company Tasks List
      # used as the method name of the controller
      operationId: getTasksList
      parameters:
        - name: body
          in: body
          description: Get Company Tasks List
          schema:
           $ref: '#/definitions/getTasksList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getOpportunitiesList:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard_ctrl
    post:
      tags: ['Dashboard Module']
      description: Get Company Opportunities List
      summary: Get Company Opportunities List
      # used as the method name of the controller
      operationId: getOpportunitiesList
      parameters:
        - name: body
          in: body
          description: getOpportunitiesList
          schema:
           $ref: '#/definitions/getOpportunitiesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getEstimatesList:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard_ctrl
    post:
      tags: ['Dashboard Module']
      description: Get Company Estimates List
      summary: Get Company Estimates List
      # used as the method name of the controller
      operationId: getEstimatesList
      parameters:
        - name: body
          in: body
          description: Get Company Estimates List
          schema:
           $ref: '#/definitions/getEstimatesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getFilterOpportunitiesList:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard_ctrl
    post:
      tags: ['Dashboard Module']
      description: Get Company filter List
      summary: Get Company filter List
      # used as the method name of the controller
      operationId: getFilterOpportunitiesList
      parameters:
        - name: body
          in: body
          description: Get Company filter List
          schema:
           $ref: '#/definitions/getFilterOpportunitiesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getFilterProjectsList:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard_ctrl
    post:
      tags: ['Dashboard Module']
      description: Get project filter List
      summary: Get project filter List
      # used as the method name of the controller
      operationId: getFilterProjectsList
      parameters:
        - name: body
          in: body
          description: Get Project filter List
          schema:
           $ref: '#/definitions/getFilterOpportunitiesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getFilterEstimatesList:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard_ctrl
    post:
      tags: ['Dashboard Module']
      description: Get Estimate filter List
      summary: Get Estimate filter List
      # used as the method name of the controller
      operationId: getFilterEstimatesList
      parameters:
        - name: body
          in: body
          description: Get Estimate filter List
          schema:
           $ref: '#/definitions/getFilterOpportunitiesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getFilterOrdersList:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard_ctrl
    post:
      tags: ['Dashboard Module']
      description: Get  filter order List
      summary: Get  filter order List
      # used as the method name of the controller
      operationId: getFilterOrdersList
      parameters:
        - name: body
          in: body
          description: Get order filter List
          schema:
           $ref: '#/definitions/getFilterOpportunitiesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getFilterInvoicesList:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard_ctrl
    post:
      tags: ['Dashboard Module']
      description: Get  filter invoice List
      summary: Get  filter invoice List
      # used as the method name of the controller
      operationId: getFilterInvoicesList
      parameters:
        - name: body
          in: body
          description: Get invoice filter List
          schema:
           $ref: '#/definitions/getFilterOpportunitiesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"  
  /api/getFilterTasksList:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard_ctrl
    post:
      tags: ['Dashboard Module']
      description: Get  filter tasks List
      summary: Get  filter tasks List
      # used as the method name of the controller
      operationId: getFilterTasksList
      parameters:
        - name: body
          in: body
          description: Get task filter List
          schema:
           $ref: '#/definitions/getFilterOpportunitiesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                
  /api/getEstimateDetail:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post:
      tags: ['Estimate Module']
      description: get detail of estimate
      summary: get detail of estimate
      # used as the method name of the controller
      operationId: getEstimateDetail
      parameters:
        - name: body
          in: body
          description: Get detail of estimation
          schema:
           $ref: '#/definitions/getEstimateDetail'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getProjectsList:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard_ctrl
    post:
      tags: ['Dashboard Module']
      description: Get Company Projects List
      summary: Get Company Projects List
      # used as the method name of the controller
      operationId: getProjectsList
      parameters:
        - name: body
          in: body
          description: Get Company Projects List
          schema:
           $ref: '#/definitions/getProjectsList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getOrdersList:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard_ctrl
    post:
      tags: ['Dashboard Module']
      description: Get Company Orders List
      summary: Get Company Orders List
      # used as the method name of the controller
      operationId: getOrdersList
      parameters:
        - name: body
          in: body
          description: Get Company Orders List
          schema:
           $ref: '#/definitions/getOrdersList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getInvoicesList:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard_ctrl
    post:
      tags: ['Dashboard Module']
      description: Get Company Invoices List
      summary: Get Company Invoices List
      # used as the method name of the controller
      operationId: getInvoicesList
      parameters:
        - name: body
          in: body
          description: Get Company Invoices List
          schema:
           $ref: '#/definitions/getInvoicesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getGoalsList:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard_ctrl
    post:
      tags: ['Dashboard Module']
      description: Get Company Goals List
      summary: Get Company Goals List
      # used as the method name of the controller
      operationId: getGoalsList
      parameters:
        - name: body
          in: body
          description: Get Company Goals List
          schema:
           $ref: '#/definitions/getGoalsList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getTailgatesList:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard_ctrl
    post:
      tags: ['Dashboard Module']
      description: Get Company Tailgates List
      summary: Get Company Tailgates List
      # used as the method name of the controller
      operationId: getTailgatesList
      parameters:
        - name: body
          in: body
          description: Get Company Tailgates List
          schema:
           $ref: '#/definitions/getTailgatesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addcompanycontact:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: '{"userId":"593fd7add9e17f2a62d6739e","companyId":"5936895125da631546bfb13e","phone":[{"phonetype":"Mobile","phone":"(222) 222-2222","isPrimary":true},{"phonetype":"Fax","phone":"(555) 555-5555","isPrimary":false}],"address":[{"addressType":"Work","mapAddress1":"AKL 25","mapAddress2":"lop","city":"JJK","state":"LOIU","zip":"12121","countryId":"1","isPrimary":true}],"internet":[{"internetType":"Work","internetvalue":"koi@kkl.net","isPrimary":true}],"companyName":"HERA","companyContactId":"","isSalesRep":true,"webAddress":"www.google.com","userType":"1","firstname":"HEMKUNT","lastname":"RAO","title":"HRA","nickName":"HKR","typeId":"594cfda576aa1b4d699152b1","statusId":"594cfcd71ee4f943464aec2e","sourceId":"5959e36b689dfd693ecab258","createdBy":"Hive","modifiedBy":"Hive"}'
      summary: Add contact for a company
      # used as the method name of the controller
      operationId: addCompanyContact
      parameters:
        - name: body
          in: body
          description: Add contact for a company
          schema:
           $ref: '#/definitions/addcompanycontact'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                           
  /api/addcompanycontactMoreinfo:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: '{"contactId":"5963183ef5ec197a8cb2ae7f","departmentId":"594cfe0076aa1b4d699152bb","industryId":"594d072375eec80f95202429","referredBy":"59522f0cfdccca571df958fd","birthday":"Invalid date","spouse":"fggf","children":"gf","branch":"g","notes":"fggf","keyword":["594d010876aa1b4d699152c1"],"modifiedBy":"Hive"}'
      summary: Add more info to contact for a company
      # used as the method name of the controller
      operationId: addCompanyContactMore
      parameters:
        - name: body
          in: body
          description: Add more info to contact for a company
          schema:
           $ref: '#/definitions/addcompanycontactmoreinfo'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getcontactDropdown:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: Get Contact Dropdown  List
      summary: Get Contact Dropdown  List
      # used as the method name of the controller
      operationId: getcontactdropdown
      parameters:
        - name: body
          in: body
          description: Get Contact Dropdown  List
          schema:
           $ref: '#/definitions/getcompanydropdownList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getIndustryList:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: Get Industry Dropdown  List
      summary: Get Industry Dropdown  List
      # used as the method name of the controller
      operationId: getIndustryList
      parameters:
        - name: body
          in: body
          description: Get Industry Dropdown  List
          schema:
           $ref: '#/definitions/getIndustryList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addgroup:
    # binds a127 app logic to a route
    x-swagger-router-controller: group_ctrl
    post:
      tags: ['Group Module']
      description: Add group for a contact 
      summary: Add group for a contact 
      # used as the method name of the controller
      operationId: addcontactGroup
      parameters:
        - name: body
          in: body
          description: Add group for a contact 
          schema:
           $ref: '#/definitions/addcontactGroup'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deletecontactGroup:
    # binds a127 app logic to a route
    x-swagger-router-controller: group_ctrl
    post:
      tags: ['Group Module']
      description: Delete group for a contact 
      summary: Delete group for a contact 
      # used as the method name of the controller
      operationId: deletecontactGroup
      parameters:
        - name: body
          in: body
          description: delete group for a contact 
          schema:
           $ref: '#/definitions/deletecontactGroup'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addcontactType:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: Add type for a contact 
      summary: Add type for a contact 
      # used as the method name of the controller
      operationId: addcontactType
      parameters:
        - name: body
          in: body
          description: Add type for a contact 
          schema:
           $ref: '#/definitions/addcontactType'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addcontactStatus:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: Add Status for a contact 
      summary: Add Status for a contact 
      # used as the method name of the controller
      operationId: addcontactStatus
      parameters:
        - name: body
          in: body
          description: Add status for a contact 
          schema:
           $ref: '#/definitions/addcontactStatus'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addcontactSource:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: Add Source for a contact 
      summary: Add Source for a contact 
      # used as the method name of the controller
      operationId: addcontactSource
      parameters:
        - name: body
          in: body
          description: Add source for a contact 
          schema:
           $ref: '#/definitions/addcontactSource'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addcontactDepartment:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: Add Department for a contact 
      summary: Add Department for a contact  
      # used as the method name of the controller
      operationId: addcontactDepartment
      parameters:
        - name: body
          in: body
          description: Add Department for a contact 
          schema:
           $ref: '#/definitions/addcontactDepartment'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addcontactIndustry:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: Add Industry for a contact 
      summary: Add Industry for a contact  
      # used as the method name of the controller
      operationId: addcontactIndustry
      parameters:
        - name: body
          in: body
          description: Add Industry for a contact 
          schema:
           $ref: '#/definitions/addcontactIndustry'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteContactDepartment:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: Delete Contact Department
      summary: Delete Contact Department  
      # used as the method name of the controller
      operationId: deleteContactDepartment
      parameters:
        - name: body
          in: body
          description: Delete Contact Department
          schema:
           $ref: '#/definitions/deleteContactDepartment'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteContactIndustry:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: Delete Contact Industry
      summary: Delete Contact Industry 
      # used as the method name of the controller
      operationId: deleteContactIndustry
      parameters:
        - name: body
          in: body
          description: Delete Contact Industry
          schema:
           $ref: '#/definitions/deleteContactIndustry'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                        
  /api/updateContactPic:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: Update Contact Profile Pic
      summary: Update Contact Profile Pic
      # used as the method name of the controller
      operationId: updateContactPic
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: id
          description: Contact Id
          type: string
          required: true                
        - in: formData
          name: file
          description: Update Contact Profile Pic
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getContactList:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: Get Company contact List
      summary: Get Company contact List
      # used as the method name of the controller
      operationId: getContactList
      parameters:
        - name: body
          in: body
          description: Get Company Tailgates List
          schema:
           $ref: '#/definitions/getContactList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getContactDetail:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: Get Company contact detail
      summary: Get Company contact detail
      # used as the method name of the controller
      operationId: getContactDetail
      parameters:
        - name: body
          in: body
          description: Get Company contact details List
          schema:
           $ref: '#/definitions/getContactDetail'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateCompanyContact:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: '{"contactId":"5963183ef5ec197a8cb2ae7f","userId":"593fd7add9e17f2a62d6739e","companyId":"5936895125da631546bfb13e","phone":[{"_id":"5963183ff5ec197a8cb2ae80","updatedAt":"2017-07-10T06:01:35.103Z","createdAt":"2017-07-10T06:01:35.103Z","deleted":false,"isPrimary":true,"phone":"(222) 222-2222","phonetype":"Mobile","contactId":"5963183ef5ec197a8cb2ae7f","userId":"593fd7add9e17f2a62d6739e","__v":0},{"_id":"5963183ff5ec197a8cb2ae81","updatedAt":"2017-07-10T06:01:35.105Z","createdAt":"2017-07-10T06:01:35.105Z","deleted":false,"isPrimary":false,"phone":"(555) 555-5555","phonetype":"Fax","contactId":"5963183ef5ec197a8cb2ae7f","userId":"593fd7add9e17f2a62d6739e","__v":0}],"address":[{"_id":"5963183ff5ec197a8cb2ae82","updatedAt":"2017-07-10T06:01:35.220Z","createdAt":"2017-07-10T06:01:35.220Z","deleted":false,"isPrimary":true,"countryId":1,"state":"LOIU","city":"JJK","zip":"12121","mapAddress2":"lop","mapAddress1":"AKL 25","addressType":"Work","contactId":"5963183ef5ec197a8cb2ae7f","userId":"593fd7add9e17f2a62d6739e","__v":0}],"internet":[{"_id":"5963183ff5ec197a8cb2ae83","updatedAt":"2017-07-10T06:01:35.611Z","createdAt":"2017-07-10T06:01:35.611Z","deleted":false,"isPrimary":true,"internetvalue":"koi@kkl.net","internetType":"Work","contactId":"5963183ef5ec197a8cb2ae7f","userId":"593fd7add9e17f2a62d6739e","__v":0}],"companyName":"HERA","contactCompanyId":"5963183ef5ec197a8cb2ae7f","isSalesRep":true,"webAddress":"www.google.com","userType":"1","firstname":"HEMKUNT","lastname":"RAO","title":"HRA","nickName":"HKR","typeId":"594cfda576aa1b4d699152b1","statusId":"594cfcd71ee4f943464aec2e","sourceId":"5959e36b689dfd693ecab258","modifiedBy":"Hive"}'
      summary: Update contact for a company
      # used as the method name of the controller
      operationId: updateCompanyContact
      parameters:
        - name: body
          in: body
          description: Update contact for a company
          schema:
           $ref: '#/definitions/updateCompanyContact'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addcontacttoGroup:
    # binds a127 app logic to a route
    x-swagger-router-controller: group_ctrl
    post:
      tags: ['Group Module']
      description: '{ groupId:587c5bf71d3c5913596e4378,contactArray:["59367bce6c8b0712003e5ec6","1224554"],userId:"2122121"}'
      summary: add contact to a group
      # used as the method name of the controller
      operationId: addcontacttoGroup
      parameters:
        - name: body
          in: body
          description: '{ groupId:587c5bf71d3c5913596e4378,contactArray:["59367bce6c8b0712003e5ec6","1224554"],userId:"2122121"}'
          schema:
           $ref: '#/definitions/addcontacttoGroup'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/removecontactfromGroup:
    # binds a127 app logic to a route
    x-swagger-router-controller: group_ctrl
    post:
      tags: ['Group Module']
      description: '{ groupId:587c5bf71d3c5913596e4378,contactArray:["59367bce6c8b0712003e5ec6","1224554"],userId:"2122121"}'
      summary: Remove contact to a group
      # used as the method name of the controller
      operationId: removecontactfromGroup
      parameters:
        - name: body
          in: body
          description: '{ groupId:587c5bf71d3c5913596e4378,contactArray:["59367bce6c8b0712003e5ec6","1224554"],userId:"2122121"}'
          schema:
           $ref: '#/definitions/removecontactfromGroup'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getcontactGroup:
    # binds a127 app logic to a route
    x-swagger-router-controller: group_ctrl
    post:
      tags: ['Group Module']
      description: 'Get group list'
      summary: get contact for a group
      # used as the method name of the controller
      operationId: getcontactGroup
      parameters:
        - name: body
          in: body
          description: 'Get group list'
          schema:
           $ref: '#/definitions/getcontactGroup'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deletecontact:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: 'Delete Company Contact'
      summary: Delete company contact
      # used as the method name of the controller
      operationId: deletecontact
      parameters:
        - name: body
          in: body
          description: 'Delete company contact'
          schema:
           $ref: '#/definitions/deletecontact'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/removecontactlist:
    # binds a127 app logic to a route
    x-swagger-router-controller: group_ctrl
    post:
      tags: ['Group Module']
      description: 'list Contact to remove from group'
      summary: list Contact to remove from group
      # used as the method name of the controller
      operationId: removecontactlist
      parameters:
        - name: body
          in: body
          description: 'list Contact to remove from group'
          schema:
           $ref: '#/definitions/removecontactlist'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addcontactlist:
    # binds a127 app logic to a route
    x-swagger-router-controller: group_ctrl
    post:
      tags: ['Group Module']
      description: 'list Contact to add for a  group'
      summary: list Contact to add for a group
      # used as the method name of the controller
      operationId: addcontactlist
      parameters:
        - name: body
          in: body
          description: 'list Contact to add for group'
          schema:
           $ref: '#/definitions/addcontactlist'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addNewkeywords:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: 'Add New keywords'
      summary: Add New keywords
      # used as the method name of the controller
      operationId: addNewkeywords
      parameters:
        - name: body
          in: body
          description: 'Add New keywords'
          schema:
           $ref: '#/definitions/addNewkeywords'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse" 
  /api/getIndividualList:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: 'Get Individual List'
      summary: Get Individual List
      # used as the method name of the controller
      operationId: getIndividualList
      parameters:
        - name: body
          in: body
          description: 'Get Individual List'
          schema:
           $ref: '#/definitions/getIndividualList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                        
  /api/addContactLinks:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_link_ctrl
    post:
      tags: ['Contact Link Module']
      description: 'Add Contact Links'
      summary: Add Contact Links
      # used as the method name of the controller
      operationId: addContactLinks
      parameters:
        - name: body
          in: body
          description: 'Add Contact Links'
          schema:
           $ref: '#/definitions/addContactLinks'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getContactLinks:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_link_ctrl
    post:
      tags: ['Contact Link Module']
      description: 'Get Contact Links'
      summary: Get Contact Links
      # used as the method name of the controller
      operationId: getContactLinks
      parameters:
        - name: body
          in: body
          description: 'Get Contact Links'
          schema:
           $ref: '#/definitions/getContactLinks'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error/DOCUMENT Modul
          schema:
            $ref: "#/definitions/ErrorResponse" 
  /api/removeContactLinks:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_link_ctrl
    post:
      tags: ['Contact Link Module']
      description: 'Remove Contact Links'
      summary: Remove Contact Links
      # used as the method name of the controller
      operationId: removeContactLinks
      parameters:
        - name: body
          in: body
          description: 'Remove Contact Links'
          schema:
           $ref: '#/definitions/removeContactLinks'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getContactLists:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_link_ctrl
    post:
      tags: ['Contact Link Module']
      description: 'Get Contact Lists'
      summary: Get Contact Lists
      # used as the method name of the controller
      operationId: getContactLists
      parameters:
        - name: body
          in: body
          description: 'Get Contact Lists'
          schema:
           $ref: '#/definitions/getContactLists'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addOpportunity:
    # binds a127 app logic to a route
    x-swagger-router-controller: opportunity_ctrl
    post:
      tags: ['Opportunity Module']
      description: Add opportunity
      summary: Add opportunity
      # used as the method name of the controller
      operationId: addOpportunity
      parameters:
        - name: body
          in: body
          description: Add opportunity for a company
          schema:
           $ref: '#/definitions/addopportunity'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateOpportunity:
    # binds a127 app logic to a route
    x-swagger-router-controller: opportunity_ctrl
    post:
      tags: ['Opportunity Module']
      description: Update Opportunity for comapny
      summary: Update Opportunity for a company
      # used as the method name of the controller
      operationId: updateOpportunity
      parameters:
        - name: body
          in: body
          description: Update contact for a company
          schema:
           $ref: '#/definitions/updateopportunity'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"           
  /api/getCategory:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: Get Category List
      summary: Get Category List
      # used as the method name of the controller
      operationId: getCategoryList
      parameters:
        - name: body
          in: body
          description: Get Contact Dropdown  List
          schema:
           $ref: '#/definitions/getcategory'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getOpportunitiesByCompany:
    # binds a127 app logic to a route
    x-swagger-router-controller: opportunity_ctrl
    post:
      tags: ['Opportunity Module']
      description: Get opportunity List
      summary: Get opportunity List
      # used as the method name of the controller
      operationId: getOpportunitiesByCompany
      parameters:
        - name: body
          in: body
          description: Get opportunity List
          schema:
           $ref: '#/definitions/opportunityByCompany'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getOpportunitiesData:
    # binds a127 app logic to a route
    x-swagger-router-controller: opportunity_ctrl
    post:
      tags: ['Opportunity Module']
      description: Get opportunity by _id to update
      summary:  Get opportunity by _id to update
      # used as the method name of the controller
      operationId: getOpportunitiesData
      parameters:
        - name: body
          in: body
          description:  Get opportunity by _id to update
          schema:
           $ref: '#/definitions/opportunityById'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteOpportunity:
    # binds a127 app logic to a route
    x-swagger-router-controller: opportunity_ctrl
    post:
      tags: ['Opportunity Module']
      description: Delete opportunity by opprtunity Id
      summary:  Delete opportunity by opprtunity Id 
      # used as the method name of the controller
      operationId: deleteOpportunity
      parameters:
        - name: body
          in: body
          description:  Delete opportunity by opprtunity Id
          schema:
           $ref: '#/definitions/deleteOpportunity'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                          
  /api/getContactOpportunities:
    # binds a127 app logic to a route
    x-swagger-router-controller: opportunity_ctrl
    post:
      tags: ['Opportunity Module']
      description: Get Contact Opportunities
      summary:  Get Contact Opportunities 
      # used as the method name of the controller
      operationId: getContactOpportunities
      parameters:
        - name: body
          in: body
          description:  Get Contact Opportunities
          schema:
           $ref: '#/definitions/getContactOpportunities'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getOpportunitiesEstimates:
    # binds a127 app logic to a route
    x-swagger-router-controller: opportunity_ctrl
    post:
      tags: ['Opportunity Module']
      description: Get Opportunities All Estimates
      summary:  Get Opportunities All Estimates 
      # used as the method name of the controller
      operationId: getOpportunitiesEstimates
      parameters:
        - name: body
          in: body
          description:  Get Opportunities All Estimates
          schema:
           $ref: '#/definitions/getOpportunitiesEstimates'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                                                  
  /api/getCompanyListByAlphabet:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: Serach company list by alphabet 
      summary: Serach company list by alphabet
      # used as the method name of the controller
      operationId: getCompanyListByAlphabet
      parameters:
        - name: body
          in: body
          description: Get Contact Dropdown List
          schema:
           $ref: '#/definitions/getcompanycontactinfo'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getItemListByAlphabet:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: Serach item list by alphabet
      summary: Serach item list by alphabet
      # used as the method name of the controller
      operationId: getItemListByAlphabet
      parameters:
        - name: body
          in: body
          description: Serach item list by alphabet
          schema:
           $ref: '#/definitions/getItemListByAlphabet'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"            
  /api/getCompanyPhoneInternet:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: Get company phone and intenet info as per company name
      summary: Get company phone and intenet info as per company name
      # used as the method name of the controller
      operationId: getCompanyPhoneInternet
      parameters:
        - name: body
          in: body
          description: Get Contact Dropdown  List
          schema:
           $ref: '#/definitions/getPhoneInternetInfo'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                
  /api/addCategory:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: Add category
      summary: Add category
      # used as the method name of the controller
      operationId: addCategory
      parameters:
        - name: body
          in: body
          description: Add category for a company
          schema:
           $ref: '#/definitions/addcategory'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getOpportunityListByAlphabet:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: Get opportunity at dropdown
      summary: Get Opportunity at dropdown
      # used as the method name of the controller
      operationId: getOpportunityListByAlphabet
      parameters:
        - name: body
          in: body
          description: get opportunity at dropdown
          schema:
           $ref: '#/definitions/getOpportunityListByAlphabet'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/opportunityNo:
    # binds a127 app logic to a route
    x-swagger-router-controller: opportunity_ctrl
    get:
      tags: ['Opportunity Module']
      description: get opportunity number 
      summary:  get opportunity number 
      # used as the method name of the controller
      operationId: opportunityNo
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/salesrep:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: get sales rep by company id
      summary: get sales rep by company id
      # used as the method name of the controller
      operationId: SalesRep
      parameters:
        - name: body
          in: body
          description: Get sales rep
          schema:
           $ref: '#/definitions/SalesRep'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getIndividual:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: get list of individual
      summary: get list of individual
      # used as the method name of the controller
      operationId: getIndividual
      parameters:
        - name: body
          in: body
          description: Get sales rep
          schema:
           $ref: '#/definitions/getIndividual'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getEstimateByAlphabet:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: get list of Estimate
      summary: get list of Estimate
      # used as the method name of the controller
      operationId: getEstimateByAlphabet
      parameters:
        - name: body
          in: body
          description: Get Estimate List
          schema:
           $ref: '#/definitions/getEstimateByAlphabet'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getDocument:
    # binds a127 app logic to a route
    x-swagger-router-controller: document_ctrl
    post:
      tags: ['Document Module']
      description: get list of Document
      summary: get list of Document
      # used as the method name of the controller
      operationId: getContactDocument
      parameters:
        - name: body
          in: body
          description: Get list of Document
          schema:
           $ref: '#/definitions/commonparams'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getEstimate:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post:
      tags: ['Estimate Module']
      description: get list of estimate
      summary: get list of estimate
      # used as the method name of the controller
      operationId: getContactEstimation
      parameters:
        - name: body
          in: body
          description: Get list of estimation
          schema:
           $ref: '#/definitions/commonparams'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addEstimate:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post:
      tags: ['Estimate Module']
      description: '{"companyId":"5936895125da631546bfb13e","customerId":"595cb9c4bbe5611d1609a25a","title":"Drone System"
,"opportunityId":"595cbdcbaed9085b7b52a94f","individualId":"595f3ce873411341a6d5e983","proposalId":"5936895125da631546bfb13e"
,"projectId":"5936895125da631546bfb13e","billingAddress1":"as","billingAddress2":"sa","billingcity":"as"
,"billingstate":"as","billingzip":"as","shippingAddress1":"sa","shippingAddress2":"as","shippingcity"
:"as","shippingstate":"as","shippingzip":"as","salesRep":"59538b8ab752d269deb46ed0","class":1,"stageId"
:3,"createdBy":"Hive","modifiedBy":"Hive","item":[{"_id":"59538349da1fec4510bd07d5","updatedAt":"2017-06-29T05
:29:23.987Z","createdAt":"2017-06-28T10:22:01.138Z","companyId":"5936895125da631546bfb13e","itemName"
:"Cable","sku":"sku","itemTypeId":{"_id":"59539c7dd7aa0634a45d3aae","itemName":"Machine"},"itemCategoryId"
:{"_id":"595383037d3d2042ece6c82c","categoryName":"Material"},"cost":20,"unitPrice":20,"project":"","quantity"
:1,"manufacturer":"DRR","assetTag":"asset","location":"smart data","description":"demo","notes":"test"
,"isTaxable":1,"__v":0,"deleted":false,"taxRate":null,"itemImage":"http://52.39.212.226:5058/images/item
/1498714163963_7918423710_e6dd168d7c_b.jpg","amount":20,"total":20.36}],"memo":"a","totalEstimate":20
.36}'
      summary: add estimate
      # used as the method name of the controller
      operationId: addEstimate
      parameters:
        - name: body
          in: body
          description: Get list of estimation
          schema:
           $ref: '#/definitions/addEstimate'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateEstimateDetail:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post:
      tags: ['Estimate Module']
      description: '{"companyId":"5936895125da631546bfb13e","estimateId":"59631d7df5ec197a8cb2ae8c","customerId":"595cb9c4bbe5611d1609a25a"
,"title":"Drone System","opportunityId":"595cbdcbaed9085b7b52a94f","individualId":"595f3ce873411341a6d5e983"
,"proposalId":"5936895125da631546bfb13e","projectId":"5936895125da631546bfb13e","billingAddress1":"as"
,"billingAddress2":"sa","billingcity":"as","billingstate":"as","billingzip":"as","shippingAddress1":"sa"
,"shippingAddress2":"as","shippingcity":"as","shippingstate":"as","shippingzip":"as","salesRep":"59538b8ab752d269deb46ed0"
,"class":1,"stage":1,"modifiedBy":"Hive","item":[{"_id":"59631d7df5ec197a8cb2ae8d","updatedAt":"2017-07-10T06
:23:57.857Z","createdAt":"2017-07-10T06:23:57.857Z","itemName":"Cable","estimateId":"59631d7df5ec197a8cb2ae8c"
,"description":"demo","quantity":1,"unitPrice":20,"total":20.36,"amount":20,"isTaxable":true,"__v":0
}],"memo":"a","totalEstimate":20.36}'
      summary: update estimate detail
      # used as the method name of the controller
      operationId: updateEstimateDetail
      parameters:
        - name: body
          in: body
          description: update estimation detail
          schema:
           $ref: '#/definitions/updateEstimateDetail'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteEstimate:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post:
      tags: ['Estimate Module']
      description: Delete estimate
      summary: Delete estimate
      # used as the method name of the controller
      operationId: deleteEstimate
      parameters:
        - name: body
          in: body
          description: Deleted estimation
          schema:
           $ref: '#/definitions/deleteEstimate'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getInvoice:
    # binds a127 app logic to a route
    x-swagger-router-controller: invoices_ctrl
    post:
      tags: ['Invoice Module']
      description: get list of Invoice
      summary: get list of Invoice
      # used as the method name of the controller
      operationId: getContactInvoice
      parameters:
        - name: body
          in: body
          description: Get list of Invoices
          schema:
           $ref: '#/definitions/commonparams'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addInvoice:
    # binds a127 app logic to a route
    x-swagger-router-controller: invoices_ctrl
    post:
      tags: ['Invoice Module']
      description: add invoice
      summary: add Invoice
      # used as the method name of the controller
      operationId: addInvoice
      parameters:
        - name: body
          in: body
          description: add Invoice
          schema:
           $ref: '#/definitions/addInvoice'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateInvoice:
    # binds a127 app logic to a route
    x-swagger-router-controller: invoices_ctrl
    post:
      tags: ['Invoice Module']
      description: update invoice
      summary: update invoice
      # used as the method name of the controller
      operationId: updateInvoice
      parameters:
        - name: body
          in: body
          description: update invoice
          schema:
           $ref: '#/definitions/updateInvoice'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getInvoiceDetails:
    # binds a127 app logic to a route
    x-swagger-router-controller: invoices_ctrl
    post:
      tags: ['Invoice Module']
      description: get  Invoice details
      summary: get  Invoice details
      # used as the method name of the controller
      operationId: getInvoiceDetails
      parameters:
        - name: body
          in: body
          description: get  Invoice details
          schema:
           $ref: '#/definitions/getInvoiceDetails'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"              
  /api/deleteInvoice:
    # binds a127 app logic to a route
    x-swagger-router-controller: invoices_ctrl
    post:
      tags: ['Invoice Module']
      description: delete Invoice
      summary: delete Invoice
      # used as the method name of the controller
      operationId: deleteInvoice
      parameters:
        - name: body
          in: body
          description: delete Invoice
          schema:
           $ref: '#/definitions/deleteInvoice'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                      
  /api/getProject:
    # binds a127 app logic to a route
    x-swagger-router-controller: project_ctrl
    post:
      tags: ['Project Module']
      description: get list of project
      summary: get list of project
      # used as the method name of the controller
      operationId: getContactProject
      parameters:
        - name: body
          in: body
          description: Get list of project
          schema:
           $ref: '#/definitions/commonparams'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getDocumentDetailByProject:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: Get document details by project
      summary: Get document details by project
      # used as the method name of the controller
      operationId: getDocumentDetailByProject
      parameters:
        - name: body
          in: body
          description: Get document details by project
          schema:
           $ref: '#/definitions/getDetailByProject'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getTimerDetailByProject:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: Get timer details by project
      summary: Get timer details by project
      # used as the method name of the controller
      operationId: getTimerDetailByProject
      parameters:
        - name: body
          in: body
          description: Get timer details by project
          schema:
           $ref: '#/definitions/getDetailByProject'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getInvoiceDetailByProject:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: Get invoice details by project
      summary: Get invoice details by project
      # used as the method name of the controller
      operationId: getInvoiceDetailByProject
      parameters:
        - name: body
          in: body
          description: Get invoice details by project
          schema:
           $ref: '#/definitions/getDetailByProject'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                         
  /api/getActivityDetailByProject:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: Get activity details by project
      summary: Get activity details by project
      # used as the method name of the controller
      operationId: getActivityDetailByProject
      parameters:
        - name: body
          in: body
          description: Get activity details by project
          schema:
           $ref: '#/definitions/getDetailByProject'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                                                                                  
  /api/getProjecttaskByAlphabet:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: Get project task by project
      summary: Get project task by project
      # used as the method name of the controller
      operationId: getProjecttaskByAlphabet
      parameters:
        - name: body
          in: body
          description: Get project task by project
          schema:
           $ref: '#/definitions/getProjecttaskByAlphabet'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                                                           
  /api/getProposal:
    # binds a127 app logic to a route
    x-swagger-router-controller: proposal_ctrl
    post:
      tags: ['Proposal Module']
      description: get list of proposal
      summary: get list of proposal
      # used as the method name of the controller
      operationId: getContactProposal
      parameters:
        - name: body
          in: body
          description: Get list of proposal for list according to proposalNumber send value in key proposalNumber e.g "proposalNumber":"E9288D0"
          schema:
           $ref: '#/definitions/commonparams'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addProposal:
    # binds a127 app logic to a route
    x-swagger-router-controller: proposal_ctrl
    post:
      tags: ['Proposal Module']
      description: Add  proposal
      summary: Add proposal
      # used as the method name of the controller
      operationId: addProposal
      parameters:
        - name: body
          in: body
          description: Add proposal
          schema:
           $ref: '#/definitions/addProposal'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                                                            
  /api/getProposalDetail:
    # binds a127 app logic to a route
    x-swagger-router-controller: proposal_ctrl
    post:
      tags: ['Proposal Module']
      description: Get proposal detail
      summary: get proposal detail
      # used as the method name of the controller
      operationId: getProposalDetail
      parameters:
        - name: body
          in: body
          description: get proposal detail
          schema:
           $ref: '#/definitions/getProposalDetail'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                                                            
  /api/updateProposalDetail:
    # binds a127 app logic to a route
    x-swagger-router-controller: proposal_ctrl
    post:
      tags: ['Proposal Module']
      description: update proposal detail
      summary: update proposal detail
      # used as the method name of the controller
      operationId: updateProposalDetail
      parameters:
        - name: body
          in: body
          description: get proposal detail
          schema:
           $ref: '#/definitions/updateProposalDetail'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteProposal:
    # binds a127 app logic to a route
    x-swagger-router-controller: proposal_ctrl
    post:
      tags: ['Proposal Module']
      description: Delete Proposal
      summary: Delete Proposal
      # used as the method name of the controller
      operationId: deleteProposal
      parameters:
        - name: body
          in: body
          description: Deleted Proposal
          schema:
           $ref: '#/definitions/deleteProposal'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/proposalSendToCustomer:
    # binds a127 app logic to a route
    x-swagger-router-controller: proposal_ctrl
    post:
      tags: ['Proposal Module']
      description: Proposal Send To Customer
      summary: Proposal Send To Customer
      # used as the method name of the controller
      operationId: proposalSendToCustomer
      parameters:
        - name: body
          in: body
          description: Proposal Send To Customer
          schema:
           $ref: '#/definitions/proposalSendToCustomer'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"            
  /api/getEmailTemplate:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: get template of proposal
      summary: get template of proposal
      # used as the method name of the controller
      operationId: getEmailTemplate
      parameters:
        - name: body
          in: body
          description: Get list of proposal
          schema:
           $ref: '#/definitions/getEmailTemplate'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                                                            
  /api/getEmailTemplatelist:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: get template of proposal
      summary: get template of proposal
      # used as the method name of the controller
      operationId: getEmailTemplatelist
      parameters:
        - name: body
          in: body
          description: Get list of proposal
          schema:
           $ref: '#/definitions/getEmailTemplatelist'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                                                            
  /api/addItemType:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: Add item type
      summary: Add item type
      # used as the method name of the controller
      operationId: addItemType
      parameters:
        - name: body
          in: body
          description: Add item type 
          schema:
           $ref: '#/definitions/additemtype'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateItemType:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: update item type
      summary: update item type
      # used as the method name of the controller
      operationId: updateItemType
      parameters:
        - name: body
          in: body
          description: update item type 
          schema:
           $ref: '#/definitions/updateItemType'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addItemCategory:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: Add item category 
      summary: Add item category
      # used as the method name of the controller
      operationId: addItemCategory
      parameters:
        - name: body
          in: body
          description: Add item category 
          schema:
           $ref: '#/definitions/additemcategory'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateItemCategory:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: update item category 
      summary: update item category
      # used as the method name of the controller
      operationId: updateItemCategory
      parameters:
        - name: body
          in: body
          description: update item category 
          schema:
           $ref: '#/definitions/updateItemCategory'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getItemType:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: Get item type list
      summary:  Get item type list
      # used as the method name of the controller
      operationId: getItemType
      parameters:
        - name: body
          in: body
          description:  Get item type list
          schema:
           $ref: '#/definitions/itemslist'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getItemCategory:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: Get item category list
      summary:  Get item category list
      # used as the method name of the controller
      operationId: getItemCategory
      parameters:
        - name: body
          in: body
          description: Get item category list
          schema:
           $ref: '#/definitions/itemslist'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getItem:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: Get item  list
      summary:  Get item  list
      # used as the method name of the controller
      operationId: getItem
      parameters:
        - name: body
          in: body
          description: Get item  list
          schema:
           $ref: '#/definitions/itemslist'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse" 
  /api/getIndividualItem:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: Get item individual item
      summary:   Get item individual item
      # used as the method name of the controller
      operationId: getIndividualItem
      parameters:
        - name: body
          in: body
          description:  Get item individual item
          schema:
           $ref: '#/definitions/individualitem'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                   
  /api/addItem:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: Add item 
      summary: Add item 
      # used as the method name of the controller
      operationId: addItem
      parameters:
        - name: body
          in: body
          description: Add item  
          schema:
           $ref: '#/definitions/additem'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateItem:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: Update items
      summary: Update items
      # used as the method name of the controller
      operationId: updateItem
      parameters:
        - name: body
          in: body
          description: Update items
          schema:
           $ref: '#/definitions/updateitems'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getProjectByName:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: Get project by name
      summary:  Get project by name
      # used as the method name of the controller
      operationId: getProjectByName
      parameters:
        - name: body
          in: body
          description: Get project by name
          schema:
           $ref: '#/definitions/projectlist'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteItem:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: Delete item by item Id
      summary: Delete item by item Id
      # used as the method name of the controller
      operationId: deleteItem
      parameters:
        - name: body
          in: body
          description:  Delete item by item Id
          schema:
           $ref: '#/definitions/individualitem'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse" 
  /api/updateItemImage:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: Update Item Image
      summary: Update Item Image
      # used as the method name of the controller
      operationId: updateItemImage
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: itemId
          description: Item Id
          type: string
          required: true                
        - in: formData
          name: file
          description: Update Item Image
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addDocumentFiles:
    # binds a127 app logic to a route
    x-swagger-router-controller: document_ctrl
    post:
      tags: ['Document Module']
      description: Add/Update document file
      summary:  Add/Update document file
      # used as the method name of the controller
      operationId: addDocumentFile
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: id
          description: Document Id
          type: string
          required: true                
        - in: formData
          name: file
          description: Add/Update document file
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addEmailFile:
    # binds a127 app logic to a route
    x-swagger-router-controller: activities_ctrl
    post:
      tags: ['Activities Module']
      description: Add email file
      summary:  Add document file
      # used as the method name of the controller
      operationId: addEmailFile
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: id
          description: mail Id
          type: string
          required: true                
        - in: formData
          name: file
          description: Add email file
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"            
  /api/addDocument:
    # binds a127 app logic to a route
    x-swagger-router-controller: document_ctrl
    post:
      tags: ['Document Module']
      description: Add document  
      summary: Add document
      # used as the method name of the controller
      operationId: addDocument
      parameters:
        - name: body
          in: body
          description: Add document
          schema:
           $ref: "#/definitions/adddocs"
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"     
  /api/updateDocument:
    # binds a127 app logic to a route
    x-swagger-router-controller: document_ctrl
    post:
      tags: ['Document Module']
      description: update Document detail
      summary: update Document detail
      # used as the method name of the controller
      operationId: updateDocument
      parameters:
        - name: body
          in: body
          description: update Document detail
          schema:
           $ref: '#/definitions/updatedocs'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteDocument:
    # binds a127 app logic to a route
    x-swagger-router-controller: document_ctrl
    post:
      tags: ['Document Module']
      description: delete Document detail
      summary: delete Document detail
      # used as the method name of the controller
      operationId: deleteDocument
      parameters:
        - name: body
          in: body
          description: delete Document detail
          schema:
           $ref: '#/definitions/deleteDocument'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getIndividualDocument:
    # binds a127 app logic to a route
    x-swagger-router-controller: document_ctrl
    post:
      tags: ['Document Module']
      description: get individual document
      summary: get individual document
      # used as the method name of the controller
      operationId: getIndividualDocument
      parameters:
        - name: body
          in: body
          description: get individual document
          schema:
           $ref: '#/definitions/docsindividual'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addDocumentCategory:
    # binds a127 app logic to a route
    x-swagger-router-controller: document_ctrl
    post:
      tags: ['Document Module']
      description: Add document category 
      summary: Add document category
      # used as the method name of the controller
      operationId: addDocumentCategory
      parameters:
        - name: body
          in: body
          description: Add document category 
          schema:
           $ref: '#/definitions/additemcategory'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getDocumentCategory:
    # binds a127 app logic to a route
    x-swagger-router-controller: document_ctrl
    post:
      tags: ['Document Module']
      description: Get document category list
      summary:  Get document category list
      # used as the method name of the controller
      operationId: getDocumentCategory
      parameters:
        - name: body
          in: body
          description: Get item category list
          schema:
           $ref: '#/definitions/itemslist'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getProjectListByAlphabet:
    # binds a127 app logic to a route
    x-swagger-router-controller: document_ctrl
    post:
      tags: ['Document Module']
      description: Get document at dropdown
      summary: Get document at dropdown
      # used as the method name of the controller
      operationId: getProjectListByAlphabet
      parameters:
        - name: body
          in: body
          description: get project at dropdown
          schema:
           $ref: '#/definitions/getOpportunityListByAlphabet'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getEstimateListByAlphabet:
    # binds a127 app logic to a route
    x-swagger-router-controller: document_ctrl
    post:
      tags: ['Document Module']
      description: Get estimate list at dropdown
      summary: Get estimate list at dropdown
      # used as the method name of the controller
      operationId: getEstimateListByAlphabet
      parameters:
        - name: body
          in: body
          description: Get estimate list at dropdown
          schema:
           $ref: '#/definitions/getOpportunityListByAlphabet'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getOrderListByAlphabet:
    # binds a127 app logic to a route
    x-swagger-router-controller: document_ctrl
    post:
      tags: ['Document Module']
      description: Get order list at dropdown
      summary: Get order list at dropdown
      # used as the method name of the controller
      operationId: getOrderListByAlphabet
      parameters:
        - name: body
          in: body
          description: Get order list at dropdown
          schema:
           $ref: '#/definitions/getOpportunityListByAlphabet'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getPoListByAlphabet:
    # binds a127 app logic to a route
    x-swagger-router-controller: document_ctrl
    post:
      tags: ['Document Module']
      description: Get purchase order list at dropdown
      summary: Get purchase order list at dropdown
      # used as the method name of the controller
      operationId: getPoListByAlphabet
      parameters:
        - name: body
          in: body
          description: Get purchase order list at dropdown
          schema:
           $ref: '#/definitions/getOpportunityListByAlphabet'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getClientListByAlphabet:
    # binds a127 app logic to a route
    x-swagger-router-controller: document_ctrl
    post:
      tags: ['Document Module']
      description: Get client list at dropdown
      summary: Get client list at dropdown
      # used as the method name of the controller
      operationId: getClientListByAlphabet
      parameters:
        - name: body
          in: body
          description: Get client list at dropdown
          schema:
           $ref: '#/definitions/getclient'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addNewTimer:
    # binds a127 app logic to a route
    x-swagger-router-controller: timer_ctrl
    post:
      tags: ['Timer Module']
      description: Add new timer
      summary: Add new timer
      # used as the method name of the controller
      operationId: addNewTimer
      parameters:
        - name: body
          in: body
          description: Add new timer
          schema:
           $ref: '#/definitions/addNewTimer'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateTimerInfo:
    # binds a127 app logic to a route
    x-swagger-router-controller: timer_ctrl
    post:
      tags: ['Timer Module']
      description: Update Timer Info
      summary: Update Timer Info
      # used as the method name of the controller
      operationId: updateTimerInfo
      parameters:
        - name: body
          in: body
          description: Update Timer Info
          schema:
           $ref: '#/definitions/updateTimerInfo'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getTimerDetails:
    # binds a127 app logic to a route
    x-swagger-router-controller: timer_ctrl
    post:
      tags: ['Timer Module']
      description: Get Timer Details
      summary: Get Timer Details
      # used as the method name of the controller
      operationId: getTimerDetails
      parameters:
        - name: body
          in: body
          description: Get Timer Details
          schema:
           $ref: '#/definitions/getTimerDetails'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getTimersList:
    # binds a127 app logic to a route
    x-swagger-router-controller: timer_ctrl
    post:
      tags: ['Timer Module']
      description: Get Timers List
      summary: Get Timers List
      # used as the method name of the controller
      operationId: getTimersList
      parameters:
        - name: body
          in: body
          description: Get Timers List
          schema:
           $ref: '#/definitions/getTimersList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteTimerInfo:
    # binds a127 app logic to a route
    x-swagger-router-controller: timer_ctrl
    post:
      tags: ['Timer Module']
      description: Delete Timer Info
      summary: Delete Timer Info
      # used as the method name of the controller
      operationId: deleteTimerInfo
      parameters:
        - name: body
          in: body
          description: Delete Timer Info
          schema:
           $ref: '#/definitions/deleteTimerInfo'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"            
  /api/addNewTailGate:
    # binds a127 app logic to a route
    x-swagger-router-controller: tailgates_ctrl
    post:
      tags: ['Tailgates Module']
      description: Add new tailgate
      summary: Add new tailgate
      # used as the method name of the controller
      operationId: addNewTailGate
      parameters:
        - name: body
          in: body
          description: Add new tailgate
          schema:
           $ref: '#/definitions/addNewTailGate'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateTailGateInfo:
    # binds a127 app logic to a route
    x-swagger-router-controller: tailgates_ctrl
    post:
      tags: ['Tailgates Module']
      description: Update tailgate info
      summary: Update tailgate info
      # used as the method name of the controller
      operationId: updateTailGateInfo
      parameters:
        - name: body
          in: body
          description: Update tailgate info
          schema:
           $ref: '#/definitions/updateTailGateInfo'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getTailGateDetails:
    # binds a127 app logic to a route
    x-swagger-router-controller: tailgates_ctrl
    post:
      tags: ['Tailgates Module']
      description: Get tailgate details info
      summary: Get tailgate details info
      # used as the method name of the controller
      operationId: getTailGateDetails
      parameters:
        - name: body
          in: body
          description: Get tailgate details info
          schema:
           $ref: '#/definitions/getTailGateDetails'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getTailGateList:
    # binds a127 app logic to a route
    x-swagger-router-controller: tailgates_ctrl
    post:
      tags: ['Tailgates Module']
      description: Get all tailgates list
      summary: Get all tailgates list
      # used as the method name of the controller
      operationId: getTailGateList
      parameters:
        - name: body
          in: body
          description: Get all tailgates list
          schema:
           $ref: '#/definitions/getTailGateList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addFaxDetails:
    # binds a127 app logic to a route
    x-swagger-router-controller: activities_ctrl
    post:
      tags: ['Activities Module']
      description: Add email details for comapny
      summary: Add email details for comapny
      # used as the method name of the controller
      operationId: addFaxDetails
      parameters:
        - name: body
          in: body
          description: Add email details for comapny
          schema:
           $ref: '#/definitions/addFaxDetails'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addCallDetails:
    # binds a127 app logic to a route
    x-swagger-router-controller: activities_ctrl
    post:
      tags: ['Activities Module']
      description: Add call details for comapny
      summary: Add call details for comapny
      # used as the method name of the controller
      operationId: addCallDetails
      parameters:
        - name: body
          in: body
          description: Add call details for comapny
          schema:
           $ref: '#/definitions/addCallDetails'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addLetterDetails:
    # binds a127 app logic to a route
    x-swagger-router-controller: activities_ctrl
    post:
      tags: ['Activities Module']
      description: Add letter details for comapny
      summary: Add letter details for comapny
      # used as the method name of the controller
      operationId: addLetterDetails
      parameters:
        - name: body
          in: body
          description: Add letter details for comapny
          schema:
           $ref: '#/definitions/addLetterDetails'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addEmailDetails:
    # binds a127 app logic to a route
    x-swagger-router-controller: activities_ctrl
    post:
      tags: ['Activities Module']
      description: Add email details for comapny
      summary: Add email details for comapny
      # used as the method name of the controller
      operationId: addEmailDetails
      parameters:
        - name: body
          in: body
          description: Add letter details for comapny
          schema:
           $ref: '#/definitions/addEmailDetails'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"          
  /api/addTaskDetails:
    # binds a127 app logic to a route
    x-swagger-router-controller: activities_ctrl
    post:
      tags: ['Activities Module']
      description: Add task details for comapny
      summary: Add task details for comapny
      # used as the method name of the controller
      operationId: addTaskDetails
      parameters:
        - name: body
          in: body
          description: Add task details for comapny
          schema:
           $ref: '#/definitions/addTaskDetails'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addEventDetails:
    # binds a127 app logic to a route
    x-swagger-router-controller: activities_ctrl
    post:
      tags: ['Activities Module']
      description: Add event details for comapny
      summary: Add event details for comapny
      # used as the method name of the controller
      operationId: addEventDetails
      parameters:
        - name: body
          in: body
          description: Add event details for comapny
          schema:
           $ref: '#/definitions/addEventDetails'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addNoteDetails:
    # binds a127 app logic to a route
    x-swagger-router-controller: activities_ctrl
    post:
      tags: ['Activities Module']
      description: Add note details for comapny
      summary: Add contact for a company
      # used as the method name of the controller
      operationId: addNoteDetails
      parameters:
        - name: body
          in: body
          description: Add contact for a company
          schema:
           $ref: '#/definitions/addNoteDetails'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                                                 
  /api/addProject:
    # binds a127 app logic to a route
    x-swagger-router-controller: project_ctrl
    post:
      tags: ['Project Module']
      description: Add project for company
      summary: Add project for company
      # used as the method name of the controller
      operationId: addProject
      parameters:
        - name: body
          in: body
          description: Add project for company
          schema:
           $ref: '#/definitions/addProject'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                                                                                                                                                    
  /api/getProjectDetail:
    # binds a127 app logic to a route
    x-swagger-router-controller: project_ctrl
    post:
      tags: ['Project Module']
      description: get Detail of project
      summary: get Detail of project
      # used as the method name of the controller
      operationId: getProjectDetail
      parameters:
        - name: body
          in: body
          description: Get detail of project
          schema:
           $ref: '#/definitions/getProjectDetail'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateProjectDetail:
    # binds a127 app logic to a route
    x-swagger-router-controller: project_ctrl
    post:
      tags: ['Project Module']
      description: to update Detail of project
      summary: to update Detail of project
      # used as the method name of the controller
      operationId: updateProjectDetail
      parameters:
        - name: body
          in: body
          description: to update detail of project
          schema:
           $ref: '#/definitions/updateProjectDetail'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteProject:
    # binds a127 app logic to a route
    x-swagger-router-controller: project_ctrl
    post:
      tags: ['Project Module']
      description: to delete project
      summary: to delete project
      # used as the method name of the controller
      operationId: deleteProject
      parameters:
        - name: body
          in: body
          description: to delete project
          schema:
           $ref: '#/definitions/deleteProject'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addProjectCategory:
    # binds a127 app logic to a route
    x-swagger-router-controller: project_ctrl
    post:
      tags: ['Project Module']
      description: to add project category
      summary: to add project category
      # used as the method name of the controller
      operationId: addProjectCategory
      parameters:
        - name: body
          in: body
          description: to add project category
          schema:
           $ref: '#/definitions/addProjectCategory'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getProjectDropdownList:
    # binds a127 app logic to a route
    x-swagger-router-controller: project_ctrl
    post:
      tags: ['Project Module']
      description: to get project category
      summary: to get project category
      # used as the method name of the controller
      operationId: getProjectDropdownList
      parameters:
        - name: body
          in: body
          description: to get project category
          schema:
           $ref: '#/definitions/getProjectDropdownList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getActivityDetails:
    # binds a127 app logic to a route
    x-swagger-router-controller: activities_ctrl
    post:
      tags: ['Activities Module']
      description: get activities 
      summary: get activities 
      # used as the method name of the controller
      operationId: getActivities
      parameters:
        - name: body
          in: body
          description: get activities 
          schema:
           $ref: '#/definitions/activityDetails'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addCompanyEmailTemplate:
    # binds a127 app logic to a route
    x-swagger-router-controller: emailTemplate_ctrl
    post:
      tags: ['Email Template Module']
      description: Add Company Email Template 
      summary: Add Company Email Template
      # used as the method name of the controller
      operationId: addCompanyEmailTemplate
      parameters:
        - name: body
          in: body
          description: Add Company Email Template
          schema:
           $ref: '#/definitions/addCompanyEmailTemplate'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse" 
  /api/getCompanyEmailTemplateDetails:
    # binds a127 app logic to a route
    x-swagger-router-controller: emailTemplate_ctrl
    post:
      tags: ['Email Template Module']
      description: Get Company Email Template Details 
      summary: Get Company Email Template Details
      # used as the method name of the controller
      operationId: getCompanyEmailTemplateDetails
      parameters:
        - name: body
          in: body
          description: Get Company Email Template Details
          schema:
           $ref: '#/definitions/getCompanyEmailTemplateDetails'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateCompanyEmailTemplate:
    # binds a127 app logic to a route
    x-swagger-router-controller: emailTemplate_ctrl
    post:
      tags: ['Email Template Module']
      description: Update Company Email Template 
      summary: Update Company Email Template 
      # used as the method name of the controller
      operationId: updateCompanyEmailTemplate
      parameters:
        - name: body
          in: body
          description: Update Company Email Template
          schema:
           $ref: '#/definitions/updateCompanyEmailTemplate'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getCompanyEmailTemplates:
    # binds a127 app logic to a route
    x-swagger-router-controller: emailTemplate_ctrl
    get:
      tags: ['Email Template Module']
      description: get edit template list
      summary: get edit template list
      # used as the method name of the controller
      operationId: getCompanyEmailTemplates
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getActivityList:
    # binds a127 app logic to a route
    x-swagger-router-controller: activities_ctrl
    post:
      tags: ['Activities Module']
      description: get activity list
      summary: get activity list 
      # used as the method name of the controller
      operationId: getActivityList
      parameters:
        - name: body
          in: body
          description: get activity list
          schema:
           $ref: '#/definitions/getContactList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteActivity:
    # binds a127 app logic to a route
    x-swagger-router-controller: activities_ctrl
    post:
      tags: ['Activities Module']
      description: Delete Activity 
      summary: Delete Activity 
      # used as the method name of the controller
      operationId: deleteActivity
      parameters:
        - name: body
          in: body
          description: Delete Activity
          schema:
           $ref: '#/definitions/deleteactivity'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addExpenses:
    # binds a127 app logic to a route
    x-swagger-router-controller: expenses_ctrl
    post:
      tags: ['Expenses Module']
      description: Add expenses 
      summary: Add expenses 
      # used as the method name of the controller
      operationId: addExpenses
      parameters:
        - name: body
          in: body
          description: Add expenses  
          schema:
           $ref: '#/definitions/addExpenses'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateExpenses:
    # binds a127 app logic to a route
    x-swagger-router-controller: expenses_ctrl
    post:
      tags: ['Expenses Module']
      description: Update expenses
      summary: Update expenses
      # used as the method name of the controller
      operationId: updateExpenses
      parameters:
        - name: body
          in: body
          description: Update expenses
          schema:
           $ref: '#/definitions/updateExpenses'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateExpenseImage:
    # binds a127 app logic to a route
    x-swagger-router-controller: expenses_ctrl
    post:
      tags: ['Expenses Module']
      description: Update Expenses Image
      summary: Update Expenses Image
      # used as the method name of the controller
      operationId: updateExpenseImage
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: expenseId
          description: expense Id
          type: string
          required: true                
        - in: formData
          name: file
          description: Update Expenses Image
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  # /api/getExpensesList:
  #   # binds a127 app logic to a route
  #   x-swagger-router-controller: expenses_ctrl
  #   get:
  #     tags: ['Expenses Module']
  #     description: get expenses list
  #     summary: get expenses list
  #     # used as the method name of the controller
  #     operationId: getExpensesList
  #     responses:
  #       "200":
  #         description: Success
  #         schema:
  #           # a pointer to a definition
  #           $ref: "#/definitions/appResponse"
  #       # responses may fall through to errors
  #       default:
  #         description: Error
  #         schema:
  #           $ref: "#/definitions/ErrorResponse"
  /api/getExpensesList:
    # binds a127 app logic to a route
    x-swagger-router-controller: expenses_ctrl
    post:
      tags: ['Expenses Module']
      description: get expenses list
      summary: get expenses list
      # used as the method name of the controller
      operationId: getExpensesList
      parameters:
        - name: body
          in: body
          description: get individual expenses
          schema:
           $ref: '#/definitions/getExpensesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"  
  /api/getIndividualExpenses:
    # binds a127 app logic to a route
    x-swagger-router-controller: expenses_ctrl
    post:
      tags: ['Expenses Module']
      description: get individual expenses
      summary: get individual expenses
      # used as the method name of the controller
      operationId: getIndividualExpenses
      parameters:
        - name: body
          in: body
          description: get individual expenses
          schema:
           $ref: '#/definitions/expensesId'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getEventsList:
    # binds a127 app logic to a route
    x-swagger-router-controller: calendar_ctrl
    post:
      tags: ['Calendar Module']
      description: Get Events List
      summary: Get Events List
      # used as the method name of the controller
      operationId: getEventsList
      parameters:
        - name: body
          in: body
          description: Get Events List
          schema:
           $ref: '#/definitions/getEventsList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getEventsListByContact:
    # binds a127 app logic to a route
    x-swagger-router-controller: calendar_ctrl
    post:
      tags: ['Calendar Module']
      description: Get Events List By Contact
      summary: Get Events List By Contact
      # used as the method name of the controller
      operationId: getEventsListByContact
      parameters:
        - name: body
          in: body
          description: Get Events List By Contact
          schema:
           $ref: '#/definitions/getEventsListByContact'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                       
  /api/addProjecttask:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: add project task
      summary: add project task
      # used as the method name of the controller
      operationId: addProjecttask
      parameters:
        - name: body
          in: body
          description: add project task
          schema:
           $ref: '#/definitions/addProjecttask'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse" 
  /api/getProjecttask:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: get project task list
      summary: get project list
      # used as the method name of the controller
      operationId: getProjecttask
      parameters:
        - name: body
          in: body
          description: get project list
          schema:
           $ref: '#/definitions/getProjecttask'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addActivityCategory:
    # binds a127 app logic to a route
    x-swagger-router-controller: activities_ctrl
    post:
      tags: ['Activities Module']
      description: Add activity category 
      summary: Add activity category
      # used as the method name of the controller
      operationId: addActivityCategory
      parameters:
        - name: body
          in: body
          description: Add activity category 
          schema:
           $ref: '#/definitions/addActivityCategory'
          required: true
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"        
  /api/getProjectExpenses:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: get project Expenses
      summary: get project Expenses
      # used as the method name of the controller
      operationId: getProjectExpenses
      parameters:
        - name: body
          in: body
          description: get project expenses
          schema:
           $ref: '#/definitions/getProjecttask'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  # /api/getActivityCategoryList:
  #   # binds a127 app logic to a route
  #   x-swagger-router-controller: activities_ctrl
  #   get:
  #     tags: ['Activities Module']
  #     description: get activity category list
  #     summary: get activity category list
  #     # used as the method name of the controller
  #     operationId: getActivityCategoryList
  #     responses:
  #       "200":
  #         description: Success
  #         schema:
  #           # a pointer to a definition
  #           $ref: "#/definitions/appResponse"
  #       # responses may fall through to errors
  #       default:
  #         description: Error
  #         schema:
  #           $ref: "#/definitions/ErrorResponse"
  /api/getActivityCategoryList:
    # binds a127 app logic to a route
    x-swagger-router-controller: activities_ctrl
    post:
      tags: ['Activities Module']
      description: get activity category list
      summary: get activity category list
      # used as the method name of the controller
      operationId: getActivityCategoryList
      parameters:
        - name: body
          in: body
          description: get activity category list
          schema:
           $ref: '#/definitions/getActivityCategoryList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  
  /api/getProjectestimate:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: get project estimate
      summary: get project estimate
      # used as the method name of the controller
      operationId: getProjectEstimate
      parameters:
        - name: body
          in: body
          description: get project estimate
          schema:
           $ref: '#/definitions/getProjecttask'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getProjectpo:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: get project po
      summary: get project po
      # used as the method name of the controller
      operationId: getProjectpo
      parameters:
        - name: body
          in: body
          description: get project po
          schema:
           $ref: '#/definitions/getProjecttask'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addPurchaseOrder:
    # binds a127 app logic to a route
    x-swagger-router-controller: purchaseorder_ctrl
    post:
      tags: ['Purchase Order Module']
      description: Add purchase order details
      summary: Add purchase order details
      # used as the method name of the controller
      operationId: addPurchaseOrder
      parameters:
        - name: body
          in: body
          description: Add PO
          schema:
           $ref: '#/definitions/addPurchaseOrder'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updatePurchaseOrder:
    # binds a127 app logic to a route
    x-swagger-router-controller: purchaseorder_ctrl
    post:
      tags: ['Purchase Order Module']
      description: update purchase order details
      summary: update purchase order details
      # used as the method name of the controller
      operationId: updatePurchaseOrder
      parameters:
        - name: body
          in: body
          description: update purchase order
          schema:
           $ref: '#/definitions/updatePurchaseOrder'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getPODetail:
    # binds a127 app logic to a route
    x-swagger-router-controller: purchaseorder_ctrl
    post:
      tags: ['Purchase Order Module']
      description: get  purchase order detail 
      summary: get purchase order detail
      # used as the method name of the controller
      operationId: getPODetail
      parameters:
        - name: body
          in: body
          description: get purchase order detail
          schema:
           $ref: '#/definitions/deletePO'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getPOlist:
    # binds a127 app logic to a route
    x-swagger-router-controller: purchaseorder_ctrl
    post:
      tags: ['Purchase Order Module']
      description: get  purchase order list 
      summary: get purchase order list
      # used as the method name of the controller
      operationId: getPOlist
      parameters:
        - name: body
          in: body
          description: get purchase order list
          schema:
           $ref: '#/definitions/getPOlist'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deletePO:
    # binds a127 app logic to a route
    x-swagger-router-controller: purchaseorder_ctrl
    post:
      tags: ['Purchase Order Module']
      description: delete purchase order 
      summary: delete purchase order 
      # used as the method name of the controller
      operationId: deletePO
      parameters:
        - name: body
          in: body
          description: delete purchase order
          schema:
           $ref: '#/definitions/deletePO'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deletePOAttach:
    # binds a127 app logic to a route
    x-swagger-router-controller: purchaseorder_ctrl
    post:
      tags: ['Purchase Order Module']
      description: delete purchase order  attachment
      summary: delete purchase order  attachment
      # used as the method name of the controller
      operationId: deletePOAttach
      parameters:
        - name: body
          in: body
          description: delete purchase order attachment
          schema:
           $ref: '#/definitions/deletePOAttach'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addAttachmentFile:
    # binds a127 app logic to a route
    x-swagger-router-controller: purchaseorder_ctrl
    post:
      tags: ['Purchase Order Module']
      description: add attachment for purchaseorder
      summary: add attachment for purchaseorder
      # used as the method name of the controller
      operationId: addAttachmentFile
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: id
          description: PurchaseOrder Id
          type: string
          required: true                
        - in: formData
          name: description
          description: description for attachment 
          type: string
          required: true                
        - in: formData
          name: file
          description: add attachment for purchaseorder
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addServiceOrder:
    # binds a127 app logic to a route
    x-swagger-router-controller: serviceOrder_ctrl
    post:
      tags: ['Service Order Module']
      description: Add service order details
      summary: Add service order details
      # used as the method name of the controller
      operationId: addServiceOrder
      parameters:
        - name: body
          in: body
          description: Add service order
          schema:
           $ref: '#/definitions/addServiceOrder'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateServiceOrder:
    # binds a127 app logic to a route
    x-swagger-router-controller: serviceOrder_ctrl
    post:
      tags: ['Service Order Module']
      description: update purchase order details
      summary: update purchase order details
      # used as the method name of the controller
      operationId: updateServiceOrder
      parameters:
        - name: body
          in: body
          description: update purchase order
          schema:
           $ref: '#/definitions/updateServiceOrder'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getServiceDetail:
    # binds a127 app logic to a route
    x-swagger-router-controller: serviceOrder_ctrl
    post:
      tags: ['Service Order Module']
      description: get  service order detail 
      summary: get service order detail
      # used as the method name of the controller
      operationId: getServiceDetail
      parameters:
        - name: body
          in: body
          description: get service order detail
          schema:
           $ref: '#/definitions/getServiceDetail'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getServiceOrderlist:
    # binds a127 app logic to a route
    x-swagger-router-controller: serviceOrder_ctrl
    post:
      tags: ['Service Order Module']
      description: get  service order list 
      summary: get service order list
      # used as the method name of the controller
      operationId: getServiceOrderlist
      parameters:
        - name: body
          in: body
          description: get service order list
          schema:
           $ref: '#/definitions/getServiceOrderlist'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteServiceOrder:
    # binds a127 app logic to a route
    x-swagger-router-controller: serviceOrder_ctrl
    post:
      tags: ['Service Order Module']
      description: delete Service order 
      summary: delete Service order 
      # used as the method name of the controller
      operationId: deleteServiceOrder
      parameters:
        - name: body
          in: body
          description: delete Service order
          schema:
           $ref: '#/definitions/deleteServiceOrder'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addDailyReport:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: Add project daily report
      summary: Add project daily report
      # used as the method name of the controller
      operationId: addDailyReport
      parameters:
        - name: body
          in: body
          description: Add project daily report
          schema:
           $ref: '#/definitions/addDailyReport'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteDailyReport:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: Delete group for a contact 
      summary: Delete group for a contact 
      # used as the method name of the controller
      operationId: deleteDailyReport
      parameters:
        - name: body
          in: body
          description: delete group for a contact 
          schema:
           $ref: '#/definitions/deleteDailyReport'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"            
  /api/getDailyReportList:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: get daily report list
      summary: get daily report list
      # used as the method name of the controller
      operationId: getDailyReportList
      parameters:
        - name: body
          in: body
          description: get daily report list
          schema:
           $ref: '#/definitions/getDailyReportList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getDailyReportDetails:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: get  individual daily report 
      summary: get  individual daily report 
      # used as the method name of the controller
      operationId: getDailyReportDetails
      parameters:
        - name: body
          in: body
          description: get individual daily report 
          schema:
           $ref: '#/definitions/getDailyReportDetails'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/newDailyReport:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: get  individual daily report 
      summary: get  individual daily report 
      # used as the method name of the controller
      operationId: newDailyReport
      parameters:
        - name: body
          in: body
          description: get individual daily report 
          schema:
           $ref: '#/definitions/newDailyReport'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"            
  /api/getTools:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: 'Get tools for project'
      summary: Get tools for project
      # used as the method name of the controller
      operationId: getTools
      parameters:
        - name: body
          in: body
          description: 'Get tools for project'
          schema:
           $ref: '#/definitions/getTools'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateTools:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: 'Update tools for project'
      summary: Update tools for project
      # used as the method name of the controller
      operationId: updateTools
      parameters:
        - name: body
          in: body
          description: 'Update tools for project'
          schema:
           $ref: '#/definitions/updateTools'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse" 
  /api/dailyReportSendToCustomer:
    # binds a127 app logic to a route
    x-swagger-router-controller: projectMore_ctrl
    post:
      tags: ['Project More Module']
      description: 'Daily Report Send To Customer'
      summary: Daily Report Send To Customer
      # used as the method name of the controller
      operationId: dailyReportSendToCustomer
      parameters:
        - name: body
          in: body
          description: 'Daily Report Send To Customer'
          schema:
           $ref: '#/definitions/dailyReportSendToCustomer'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getUploadItemFile:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: CSV File upload
      summary: CSV File upload
      # used as the method name of the controller
      operationId: getUploadItemFile
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: id
          description: Company Id
          type: string
        - in: formData
          name: createdBy
          description: createdBy
          type: string
        - in: formData
          name: itemTypeId
          description: itemTypeId
          type: string
        - in: formData
          name: file
          description: Update CSV for item
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getlaborRate:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: 'get Labour Rate'
      summary: get Labour Rate
      # used as the method name of the controller
      operationId: getlaborRate
      parameters:
        - name: body
          in: body
          description: 'get Labour Rate'
          schema:
           $ref: '#/definitions/getlaborRate'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addlaborRate:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: 'add Labour Rate'
      summary: add Labour Rate
      # used as the method name of the controller
      operationId: addlaborRate
      parameters:
        - name: body
          in: body
          description: 'add Labour Rate'
          schema:
           $ref: '#/definitions/addlaborRate'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/countForCompany:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: 'count For Company'
      summary: count For Company
      # used as the method name of the controller
      operationId: countForCompany
      parameters:
        - name: body
          in: body
          description: 'count For Company'
          schema:
           $ref: '#/definitions/countForCompany'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getProposalList:
    # binds a127 app logic to a route
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: 'get project list'
      summary: get project list
      # used as the method name of the controller
      operationId: geProposalList
      parameters:
        - name: body
          in: body
          description: 'get project list'
          schema:
           $ref: '#/definitions/geProposalList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getCompanyContactList:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: Get Company contact List
      summary: Get Company contact List
      # used as the method name of the controller
      operationId: getCompanyContactList
      parameters:
        - name: body
          in: body
          description: Get Company CompanyContact List
          schema:
           $ref: '#/definitions/getCompanyContactList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getAssociateContactList:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: Get Company Associate Contact List
      summary: Get Company Associate Contact List
      # used as the method name of the controller
      operationId: getAssociateContactList
      parameters:
        - name: body
          in: body
          description: Get Company Associate Contact List
          schema:
           $ref: '#/definitions/getAssociateContactList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addItemRevision:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post:
      tags: ['Estimate Module']
      description: Add Item Revision 
      summary: Add Item Revision
      # used as the method name of the controller
      operationId: addItemRevision
      parameters:
        - name: body
          in: body
          description: Add Item Revision 
          schema:
           $ref: '#/definitions/addItemRevision'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateItemRevision:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post :
      tags: ['Estimate Module']
      description: Update Item Revision 
      summary: Update Item Revision
      # used as the method name of the controller
      operationId: updateItemRevision
      parameters:
        - name: body
          in: body
          description: Update Item Revision 
          schema:
           $ref: '#/definitions/addItemRevision'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getRevisionList:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post:
      tags: ['Estimate Module']
      description: get Revision list 
      summary: get Revision list
      # used as the method name of the controller
      operationId: getRevisionList
      parameters:
        - name: body
          in: body
          description: get Revision list
          schema:
           $ref: '#/definitions/getRevisionList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getRevisionDetail:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post:
      tags: ['Estimate Module']
      description: get Revision Detail 
      summary: get Revision Detail
      # used as the method name of the controller
      operationId: getRevisionDetail
      parameters:
        - name: body
          in: body
          description: get Revision Detail
          schema:
           $ref: '#/definitions/getRevisionDetail'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteItemRevision:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post:
      tags: ['Estimate Module']
      description: delete Revision Detail 
      summary: delete Revision Detail
      # used as the method name of the controller
      operationId: deleteItemRevision
      parameters:
        - name: body
          in: body
          description: delete Revision Detail
          schema:
           $ref: '#/definitions/deleteItemRevision'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addMemoOpportunity:
    # binds a127 app logic to a route
    x-swagger-router-controller: opportunity_ctrl
    post:
      tags: ['Opportunity Module']
      description: add memo 
      summary: add Memo
      # used as the method name of the controller
      operationId: addMemoOpportunity
      parameters:
        - name: body
          in: body
          description: add Memo
          schema:
           $ref: '#/definitions/addMemo'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addMemoEstimate:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post:
      tags: ['Estimate Module']
      description: add memo 
      summary: add Memo
      # used as the method name of the controller
      operationId: addMemoEstimate
      parameters:
        - name: body
          in: body
          description: add Memo
          schema:
           $ref: '#/definitions/addMemo'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addMemoProject:
    # binds a127 app logic to a route
    x-swagger-router-controller: project_ctrl
    post:
      tags: ['Project Module']
      description: add memo 
      summary: add Memo
      # used as the method name of the controller
      operationId: addMemoProject
      parameters:
        - name: body
          in: body
          description: add Memo
          schema:
           $ref: '#/definitions/addMemo'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/exportEstimateCsv:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post:
      tags: ['Estimate Module']
      summary: exportEstimateCsv
      # used as the method name of the controller
      operationId: exportEstimateCsv
      parameters:
        - name: body
          in: body
          description: exportEstimateCsv
          schema:
           $ref: '#/definitions/exportEstimateCsv'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addMemoServiceOrder:
    # binds a127 app logic to a route
    x-swagger-router-controller: serviceOrder_ctrl
    post:
      tags: ['Service Order Module']
      description: add memo 
      summary: add Memo
      # used as the method name of the controller
      operationId: addMemoServiceOrder
      parameters:
        - name: body
          in: body
          description: add Memo
          schema:
           $ref: '#/definitions/addMemo'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addOrderType:
    # binds a127 app logic to a route
    x-swagger-router-controller: serviceOrder_ctrl
    post:
      tags: ['Service Order Module']
      description: add order type 
      summary: add order type
      # used as the method name of the controller
      operationId: addOrderType
      parameters:
        - name: body
          in: body
          description: add order type
          schema:
           $ref: '#/definitions/addOrderType'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getOrderType:
    # binds a127 app logic to a route
    x-swagger-router-controller: serviceOrder_ctrl
    post:
      tags: ['Service Order Module']
      description: get order type list
      summary: get order type list 
      # used as the method name of the controller
      operationId: getOrderType
      parameters:
        - name: body
          in: body
          description: get order type list
          schema:
           $ref: '#/definitions/getOrderType'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getManufacturerByName:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: get Manufacturer By Name
      summary: get Manufacturer By Name
      # used as the method name of the controller
      operationId: getManufacturerByName
      parameters:
        - name: body
          in: body
          description: get Manufacturer By Name
          schema:
           $ref: '#/definitions/getManufacturerByName'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addSignatureServiceOrder:
    # binds a127 app logic to a route
    x-swagger-router-controller: signature_ctrl
    post:
      tags: ['Signature Module']
      description: get sign of user
      summary: get sign of user
      # used as the method name of the controller
      operationId: addSignatureServiceOrder
      parameters:
        - name: body
          in: body
          description: get sign of user
          schema:
           $ref: '#/definitions/addSignatureServiceOrder'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getRole:
    # binds a127 app logic to a route
    x-swagger-router-controller: users
    post:
      tags: ['User Management']
      description: get roles for user
      summary: get roles for user
      # used as the method name of the controller
      operationId: getRole
      parameters:
        - name: body
          in: body
          description: get roles for user
          schema:
           $ref: '#/definitions/getRole'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateUserDetail:
    # binds a127 app logic to a route
    x-swagger-router-controller: users
    post:
      tags: ['User Management']
      description: associate role  to user
      summary: associate role  to user
      # used as the method name of the controller
      operationId: updateUserDetail
      parameters:
        - name: body
          in: body
          description: associte role  to user
          schema:
           $ref: '#/definitions/updateUserDetail'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/removeUserPic:
    # binds a127 app logic to a route
    x-swagger-router-controller: users
    post:
      tags: ['User Management']
      description: remove user pic
      summary: remove User Pic
      # used as the method name of the controller
      operationId: removeUserPic
      parameters:
        - name: body
          in: body
          description: remove User Pic
          schema:
           $ref: '#/definitions/removeUserPic'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/removeContactPic:
    # binds a127 app logic to a route
    x-swagger-router-controller: contact_ctrl
    post:
      tags: ['Contact Module']
      description: remove user pic
      summary: remove Contact Pic
      # used as the method name of the controller
      operationId: removeContactPic
      parameters:
        - name: body
          in: body
          description: remove Contact Pic
          schema:
           $ref: '#/definitions/removeContactPic'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/removeItemPic:
    # binds a127 app logic to a route
    x-swagger-router-controller: item_ctrl
    post:
      tags: ['Item Module']
      description: remove item pic
      summary: remove item Pic
      # used as the method name of the controller
      operationId: removeItemPic
      parameters:
        - name: body
          in: body
          description: remove item Pic
          schema:
           $ref: '#/definitions/removeItemPic'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateUserPic:
    # binds a127 app logic to a route
    x-swagger-router-controller: users
    post:
      tags: ['User Management']
      description: Update User Profile Pic
      summary: Update User Profile Pic
      # used as the method name of the controller
      operationId: updateUserPic
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: id
          description: Contact Id
          type: string
          required: true                
        - in: formData
          name: file
          description: Update user Profile Pic
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/checkToken:
    # binds a127 app logic to a route
    x-swagger-router-controller: users
    post:
      tags: ['User Management']
      description: remove user pic
      summary: checkToken
      # used as the method name of the controller
      operationId: checkToken
      parameters:
        - name: body
          in: body
          description: checkToken
          schema:
           $ref: '#/definitions/checkToken'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updatePic:
    x-swagger-router-controller: common_ctrl
    post:
      tags: ['Common Module']
      description: Update Contact Profile Pic
      summary: Update Contact Profile Pic
      # used as the method name of the controller
      operationId: updatePic
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: id
          description: refrence Id
          type: string
          required: true                
        - in: formData
          name: modelName
          description: model name
          type: string
          required: true     
        - in: formData
          name: file
          description: Update Contact  Pic
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addSignatureSalesrep:
    # binds a127 app logic to a route
    x-swagger-router-controller: signature_ctrl
    post:
      tags: ['Signature Module']
      description: get sign of salesRep
      summary: get sign of salesRep
      # used as the method name of the controller
      operationId: addSignatureSalesrep
      parameters:
        - name: body
          in: body
          description: get sign of salesRep
          schema:
           $ref: '#/definitions/addSignatureSalesrep'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getItemConfiglist:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimateConfiguration_ctrl
    post:
      tags: ['Estimate Configuration Module']
      description: get Item Config list
      summary: get Item Config list
      # used as the method name of the controller
      operationId: getItemConfiglist
      parameters:
        - name: body
          in: body
          description: get get Item Configlist
          schema:
           $ref: '#/definitions/getItemConfiglist'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/generateEstimatePdf:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimateConfiguration_ctrl
    post:
      tags: ['Estimate Configuration Module']
      description: genrate estimate item  pdf
      summary: genrate estimate item  pdf
      # used as the method name of the controller
      operationId: generateEstimatePdf
      parameters:
        - name: body
          in: body
          description: genrate estimate item  pdf
          schema:
           $ref: '#/definitions/generateEstimatePdf'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addEstimateFormat:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimateConfiguration_ctrl
    post:
      tags: ['Estimate Configuration Module']
      description:  Add format for estimate
      summary:  Add format for estimate
      # used as the method name of the controller
      operationId: addEstimateFormat
      parameters:
        - name: body
          in: body
          description:  Add format for estimate
          schema:
           $ref: '#/definitions/addEstimateFormat'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateEstimateFormat:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimateConfiguration_ctrl
    post:
      tags: ['Estimate Configuration Module']
      description:  use to update format for estimate
      summary:  use to update format for estimate
      # used as the method name of the controller
      operationId: updateEstimateFormat
      parameters:
        - name: body
          in: body
          description:  use to update format for estimate
          schema:
           $ref: '#/definitions/updateEstimateFormat'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addOpportunityTag:
    # binds a127 app logic to a route
    x-swagger-router-controller: opportunity_ctrl
    post:
      tags: ['Opportunity Module']
      description:  use to add opportunity tag
      summary:  use to update format for estimate
      # used as the method name of the controller
      operationId: addOpportunityTag
      parameters:
        - name: body
          in: body
          description:  use to add opportunity tag
          schema:
           $ref: '#/definitions/addOpportunityTag'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getOpportunityTag:
    # binds a127 app logic to a route
    x-swagger-router-controller: opportunity_ctrl
    get:
      tags: ['Opportunity Module']
      description: get opportunity tag 
      summary:  get opportunity tag 
      # used as the method name of the controller
      operationId: getOpportunityTag
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addSignatureImageSalesrep:
    # binds a127 app logic to a route
    x-swagger-router-controller: signature_ctrl
    post:
      tags: ['Signature Module']
      description: get sign image of salesRep
      summary: get sign image of salesRep 
      # used as the method name of the controller
      operationId: addSignatureImageSalesrep
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: id
          description: Contact Id
          type: string
          required: true                
        - in: formData
          name: file
          description: get sign image of salesRep
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateItemRevisionName:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post :
      tags: ['Estimate Module']
      description: Update Item Revision name 
      summary: Update Item Revision name
      # used as the method name of the controller
      operationId: updateItemRevisionName
      parameters:
        - name: body
          in: body
          description: Update Item Revision name
          schema:
           $ref: '#/definitions/updateItemRevisionName'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getEstimateRevisionCount:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post:
      tags: ['Estimate Module']
      description: get Estimate item revision count 
      summary: get Estimate item revision count
      # used as the method name of the controller
      operationId: getEstimateRevisionCount
      parameters:
        - name: body
          in: body
          description: get Estimate item revision count 
          schema:
           $ref: '#/definitions/getEstimateRevisionCount'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateEstimateItemName:
    # binds a127 app logic to a route
    x-swagger-router-controller: estimate_ctrl
    post:
      tags: ['Estimate Module']
      description: update Estimate item name 
      summary: update Estimate item name 
      # used as the method name of the controller
      operationId: updateEstimateItemName
      parameters:
        - name: body
          in: body
          description: update Estimate item name 
          schema:
           $ref: '#/definitions/updateEstimateItemName'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadExtronFile:
    # binds a127 app logic to a route
    x-swagger-router-controller: materialCSV_ctrl
    post:
      tags: ['Material CSV Module']
      description: CSV File upload
      summary: CSV File upload
      # used as the method name of the controller
      operationId: uploadExtronFile
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: id
          description: Company Id
          type: string
        - in: formData
          name: createdBy
          description: createdBy
          type: string
        - in: formData
          name: itemTypeId
          description: itemTypeId
          type: string
        - in: formData
          name: file
          description: Update CSV for item
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadCrestronFile:
    # binds a127 app logic to a route
    x-swagger-router-controller: materialCSV_ctrl
    post:
      tags: ['Material CSV Module']
      description: CSV File upload
      summary: CSV File upload
      # used as the method name of the controller
      operationId: uploadCrestronFile
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: id
          description: Company Id
          type: string
        - in: formData
          name: createdBy
          description: createdBy
          type: string
        - in: formData
          name: itemTypeId
          description: itemTypeId
          type: string
        - in: formData
          name: file
          description: Update CSV for item
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadCrestronXlsxFile:
    # binds a127 app logic to a route
    x-swagger-router-controller: materialCSV_ctrl
    post:
      tags: ['Material CSV Module']
      description: Xlsx File upload
      summary: Xlsx File upload
      # used as the method name of the controller
      operationId: uploadCrestronXlsxFile
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: id
          description: Company Id
          type: string
        - in: formData
          name: createdBy
          description: createdBy
          type: string
        - in: formData
          name: itemTypeId
          description: itemTypeId
          type: string
        - in: formData
          name: file
          description: Xlsx CSV for item
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadExtronFileXlsxFile:
    # binds a127 app logic to a route
    x-swagger-router-controller: materialCSV_ctrl
    post:
      tags: ['Material CSV Module']
      description: Xlsx File upload
      summary: Xlsx File upload
      # used as the method name of the controller
      operationId: uploadExtronFileXlsxFile
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: id
          description: Company Id
          type: string
        - in: formData
          name: createdBy
          description: createdBy
          type: string
        - in: formData
          name: itemTypeId
          description: itemTypeId
          type: string
        - in: formData
          name: file
          description: Xlsx xlsx for item
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadMultipleFile:
    # binds a127 app logic to a route
    x-swagger-router-controller: fileAttachments_ctrl
    post:
      tags: ['File Attachment Module']
      description:  File upload
      summary:  File upload
      # used as the method name of the controller
      operationId: uploadMultipleFile
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: id
          description: Opportunity Id
          type: string
        - in: formData
          name: uploader
          description: uploader name
          type: string
        - in: formData
          name: createdBy
          description: createdBy 
          type: string
        - in: formData
          name: modifiedBy
          description: modifiedBy
          type: string
        - in: formData
          name: file
          description: file upload
          type: file
          required: true                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getFileList:
    # binds a127 app logic to a route
    x-swagger-router-controller: fileAttachments_ctrl
    post:
      tags: ['File Attachment Module']
      description: Get files List
      summary: Get files List
      # used as the method name of the controller
      operationId: getFileList
      parameters:
        - name: body
          in: body
          description: Get files List
          schema:
           $ref: '#/definitions/getFileList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/renameFileAttachment:
    # binds a127 app logic to a route
    x-swagger-router-controller: fileAttachments_ctrl
    post:
      tags: ['File Attachment Module']
      description: Rename file
      summary: Rename file
      # used as the method name of the controller
      operationId: renameFileAttachment
      parameters:
        - name: body
          in: body
          description: Rename File
          schema:
           $ref: '#/definitions/renameFileAttachment'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteFileAttachment:
    # binds a127 app logic to a route
    x-swagger-router-controller: fileAttachments_ctrl
    post:
      tags: ['File Attachment Module']
      description: delete file
      summary: delete file
      # used as the method name of the controller
      operationId: deleteFileAttachment
      parameters:
        - name: body
          in: body
          description: delete File
          schema:
           $ref: '#/definitions/deleteFileAttachment'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/restoreFileAttachment:
    # binds a127 app logic to a route
    x-swagger-router-controller: fileAttachments_ctrl
    post:
      tags: ['File Attachment Module']
      description: restore file
      summary: restore file
      # used as the method name of the controller
      operationId: restoreFileAttachment
      parameters:
        - name: body
          in: body
          description: restore File
          schema:
           $ref: '#/definitions/restoreFileAttachment'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /swagger:
    x-swagger-pipe: swagger_raw
# complex objects have schema definitions
definitions:
  userAuth:
    type: object
    properties:
      email:
        type: string
      password:
        type: string
      grant_type:
        type: string        
  signIn:
    type: object
    properties:
      auth_user_name:
        type: string
      auth_user_id:
        type: string 
      company: 
        type: string
      userType: 
        type: string                        
  getTasksList:  
    type: object
    properties:
      companyId:
        type: string 
      per_page:
        type: number
      page:
        type: number        
  getOpportunitiesList:  
    type: object
    properties:
      companyId:
        type: string 
      per_page:
        type: number
      page:
        type: number
  getEstimatesList:  
    type: object
    properties:
      companyId:
        type: string 
      per_page:
        type: number
      page:
        type: number
  getProjectsList:  
    type: object
    properties:
      companyId:
        type: string 
      per_page:
        type: number
      page:
        type: number
  getOrdersList:  
    type: object
    properties:
      companyId:
        type: string 
      per_page:
        type: number
      page:
        type: number
  getInvoicesList:  
    type: object
    properties:
      companyId:
        type: string 
      per_page:
        type: number
      page:
        type: number
  getGoalsList:  
    type: object
    properties:
      companyId:
        type: string 
      per_page:
        type: number
      page:
        type: number
  getTailgatesList:  
    type: object
    properties:
      companyId:
        type: string 
      per_page:
        type: number
      page:
        type: number                                                                
  appResponse:
    required:
      - code
    properties:
      code:
        type: number
      message:
        type: string
  ErrorResponse:
    required:
      - message
    properties:
      message:
        type: string
  addcompanycontact:
    type: object
    properties:
      userId:
        type: string
      companyId:
        type: string
      companyContactId:
        type: string    
      companyName:
        type: string
      userType:
        type: number     
      firstname:
        type: string
      lastname:
        type: string
      title:
        type: string  
      nickName:
        type: string
      typeId:
        type: string
      webAddress:
        type: string
      statusId:
        type: string
      isSalesRep:
        type: boolean
      sourceId:
        type: string
      createdBy:
        type: string
  addcompanycontactmoreinfo:
    type: object
    properties:
      contactId:
        type: string
      departmentId:
        type: string
      industryId:
        type: string
      referredBy:
        type: string     
      birthday:
        type: string
      branch:
        type: string
      notes:
        type: string
      spouse:
        type: string
      children:
        type: string
      modifiedBy:
        type: string         
  getcompanydropdownList:  
    type: object
    properties:
      companyId:
        type: string 
      referredBy:
        type: string
  addcontactGroup:
    type: object
    properties:
      companyId:
        type: string
      userId:
        type: string  
      groupName:
        type: string
  addcontactType:
    type: object
    properties:
      companyId:
        type: string
      userId:
        type: string  
      typeName:
        type: string 
  addcontactStatus:
    type: object
    properties:
      companyId:
        type: string
      userId:
        type: string  
      statusName:
        type: string
  addcontactSource:
    type: object
    properties:
      companyId:
        type: string
      userId:
        type: string  
      sourceName:
        type: string
  addcontactDepartment:
    type: object
    properties:
      companyId:
        type: string
      userId:
        type: string  
      departmentName:
        type: string
  addcontactIndustry:
    type: object
    properties:
      companyId:
        type: string
      userId:
        type: string  
      industryName:
        type: string
  deleteContactDepartment:
    type: object
    properties:
      departmentId:
        type: string
  deleteContactIndustry:
    type: object
    properties:
      industryId:
        type: string                
  deletecontactGroup:
    type: object
    properties:
      groupId:
        type: string
  getContactList:
    type: object
    properties:
      companyId:
        type: string
      userType:
        type: number
      per_page:
        type: number
      page:
        type: number
      companySearchText:
        type: string
  getContactDetail:
    type: object
    properties:
      contactId:
        type: string
  updateCompanyContact:
    type: object
    properties:
      contactId:
        type: string
      userId:
        type: string
      companyId:
        type: string
      companyContactId:
        type: string    
      companyName:
        type: string
      userType:
        type: number     
      firstname:
        type: string
      lastname:
        type: string
      title:
        type: string  
      nickName:
        type: string
      typeId:
        type: string
      webAddress:
        type: string
      statusId:
        type: string
      isSalesRep:
        type: boolean
      sourceId:
        type: string
      createdBy:
        type: string    
      modifieBy:
        type: string
  addcontacttoGroup:
    type: object
    properties:
      groupId:
        type: string
      contactId:
        type: string
  removecontactfromGroup:
    type: object
    properties:
      groupId:
        type: string
      contactId:
        type: string
  getcontactGroup:
    type: object
    properties:
      companyId:
        type: string
  deletecontact:
    type: object
    properties:
      contactId:
        type: string
  removecontactlist:
    type: object
    properties:
      groupId:
        type: string
  addcontactlist:
    type: object
    properties:
      groupId:
        type: string
      companyId:
        type: string
  addNewkeywords:
    type: object
    properties:
      keyword:
        type: string
      companyId:
        type: string
      userId:
        type: string
  getIndividualList:
    type: object
    properties:
      companyId:
        type: string                         
  addContactLinks:  
    type: object
    properties:
      companyId:
        type: string    
      contactId:
        type: string 
      linkContactId:
        type: string
  getContactLinks:  
    type: object
    properties:
      companyId:
        type: string    
      contactId:
        type: string  
  removeContactLinks:  
    type: object
    properties:
      companyId:
        type: string    
      contactId:
        type: string    
      linkContactId:
        type: string
  getContactLists:  
    type: object
    properties:
      companyId:
        type: string
      contactId:
        type: string
  addopportunity:
    type: object
    properties:                                         
      companyId:
        type: string    
      contactId:
        type: string                   
      title:
        type: string
      createDate:
        type: string
        # format: date
      estCloseDate:
        type: string
        # format: date
      actCloseDate:
        type: string
        # format: date
      opportunityNumber:
        type: number                                
      individualId:
        type: string
      categoryId:
        type: string     
      departmentId:
        type: string     
      salesRep:
        type: string
      priorityId:
        type: number
      stageId:
        type: number
      probabilityId:
        type: number   
      description:
        type: string
      weightedValue: 
        type: number
      value: 
        type: number  
      daysOpen:
        type: number
  updateopportunity:
    type: object
    properties:
      opportunityId:
        type: string                                         
      companyId:
        type: string    
      contactId:
        type: string                   
      title:
        type: string
      createDate:
        type: string
      estCloseDate:
        type: string
      actCloseDate:
        type: string 
      opportunityNumber:
        type: number  
      individualId:
        type: string
      categoryId:
        type: string     
      departmentId:
        type: string     
      salesRep:
        type: string
      priorityId:
        type: number
      stageId:
        type: number
      probabilityId:
        type: number       
      description:
        type: string
      weightedValue: 
        type: number
      value: 
        type: number  
      daysOpen:
        type: number 
  addcategory:
    type: object
    properties:
      companyId:
        type: string
      userId:
        type: string  
      categoryName:
        type: string
  getcategory:
    type: object
    properties:
      companyId:
        type: string      
  getcompanycontactinfo:
    type: object
    properties:
      companyName:
        type: string
      companyId:
        type: string
  getItemListByAlphabet:
    type: object
    properties:
      itemName:
        type: string
      companyId:
        type: string        
  getPhoneInternetInfo:
    type: object
    properties:
      contactId:
        type: string
  SalesRep:
    type: object
    properties: 
      companyId:
        type: string
      firstname:
        type: string
  getIndividual:
    type: object
    properties: 
      companyId:
        type: string
      firstname:
        type: string
      contactId:
        type: string
  opportunityByCompany:
    type: object
    properties: 
      companyId:
        type: string
  opportunityById:
    type: object
    properties: 
      opportunityId:
        type: string
  deleteOpportunity:
    type: object
    properties: 
      opportunityId:
        type: string 	
  getContactOpportunities:
    type: object
    properties: 
      contactId:
        type: string
      companyId:
        type: string
  getOpportunitiesEstimates:
    type: object
    properties: 
      companyId:
        type: string
      opportunityId:
        type: string                         	
  commonparams:
    type: object
    properties:
      companyId:
        type: string
      contactId:
        type: string
  addEstimate:
    type: object
    properties:       
      title:
        type: string        
      billingAddress1:
        type: string        
      billingAddress2:
        type: string        
      billingcity:
        type: string        
      billingstate:
        type: string        
      billingzip:
        type: string        
      shipingAddress1:
        type: string        
      shipingAddress2:
        type: string        
      shipingcity:
        type: string        
      shipingstate:
        type: string        
      shipingzip:
        type: string        
      projectId:
        type: string     
      salesRep:
        type: string      
      memo:
        type: string       
      totalEstimate:
        type: number        
      stage:
        type: number        
      createdBy:
        type: string
  updateEstimateDetail:
    type: object
    properties:
      title:
        type: string        
      billingAddress1:
        type: string        
      billingAddress2:
        type: string        
      billingcity:
        type: string        
      billingstate:
        type: string        
      billingzip:
        type: string        
      shipingAddress1:
        type: string        
      shipingAddress2:
        type: string        
      shipingcity:
        type: string        
      shipingstate:
        type: string        
      shipingzip:
        type: string        
      projectId:
        type: string      
      totalEstimate:
        type: number        
      stage:
        type: number        
      modifiedBy:
        type: string
  getOpportunityListByAlphabet:
    type: object
    properties:
      title:
        type: string
      companyId:
        type: string
  getEstimateDetail:
    type: object
    properties:
      estimateId:
        type: string
  deleteEstimate:
    type: object
    properties:
      estimateId:
        type: string
  getEmailTemplate:
    type: object
    properties:
      code:
        type: string
  getEmailTemplatelist:
    type: object
    properties:
      subject:
        type: string
  addProposal:
    type: object
    properties:
      companyId:
        type: string
      estimateId:
        type: string
      individualId:
        type: string
      contactId:
        type: string
      customerId:
        type: string
      title:
        type: string
      discription:
        type: string
  getProposalDetail:
    type: object
    properties:
      proposalId:
        type: string
  updateProposalDetail:
    type: object
    properties:
      proposalId:
        type: string
      proposalNumber:
        type: string
      companyId:
        type: string
      estimateId:
        type: string
      individualId:
        type: string
      contactId:
        type: string
      customerId:
        type: string
      title:
        type: string
      discription:
        type: string
  deleteProposal:
    type: object
    properties:
      proposalId:
        type: string
  proposalSendToCustomer:
    type: object
    properties:
      companyId:
        type: string
      proposalId:
        type: string                
  additemtype:      
    type: object
    properties:
      companyId:
        type: string
      itemType:
        type: string 
  updateItemType:      
    type: object
    properties:
      itemTypeId:
        type: string
      itemType:
        type: string 
  additemcategory:
    type: object
    properties:
      companyId:
        type: string
      categoryType:
        type: string
  updateItemCategory:
    type: object
    properties:
      itemCategoryId:
        type: string
      categoryType:
        type: string
  itemslist:
    type: object
    properties:
      companyId:
        type: string
      per_page:
        type: number
      page:
        type: number
  additem:
    type: object
    properties:
      companyId:
        type: string
      itemName:
        type: string
      sku:
        type: string  
      itemTypeId:
        type: string             
      itemCategoryId:
        type: string  
      cost:
        type: number
      profileImage:
        type: string   
      unitPrice:
        type: number     
      project: 
        type: string
      quantity: 
        type: number
      manufacturer: 
        type: string
      assetTag: 
        type : string
      location: 
        type: string
      description: 
        type : string
      notes: 
        type: string
  updateitems:
    type: object
    properties:
      itemId:
        type: string
      companyId:
        type: string    
      itemName:
        type: string
      sku:
        type: string  
      itemTypeId:
        type: string             
      itemCategoryId:
        type: string  
      cost:
        type: number
      profileImage:
        type: string   
      unitPrice:
        type: number     
      project: 
        type: string
      quantity: 
        type: number
      manufacturer: 
        type: string
      assetTag: 
        type : string
      location: 
        type: string
      description: 
        type : string
      notes: 
        type: string
  projectlist:
    type: object
    properties:
      companyId:
        type: string
      projectName:
        type: string
  individualitem:
    type: object
    properties:
      itemId:
        type: string
  adddocs:
    type: object
    properties:
      companyId:
        type: string
      contactId:
        type: string
      clientId:
        type: string  
      projectId:
        type: string             
      opportunityId:
        type: string  
      estimateId:
        type: string
      orderId:
        type: string   
      poId:
        type: string     
      description: 
        type: string
      documentType: 
        type: string
      documentCategoryId: 
        type: string
      referenceNumber: 
        type : string
      version: 
        type: string
      location: 
        type : string
      fileName: 
        type: string
      author: 
        type : string
      pages: 
        type : number  
      keyword: 
        type: string
      documentTitle: 
        type: string
      createdBy:
        type: string  
  updatedocs:
    type: object
    properties:
      documentId:
        type: string  
      companyId:
        type: string
      contactId:
        type: string
      clientId:
        type: string  
      projectId:
        type: string             
      opportunityId:
        type: string
      estimateId:
        type: string
      orderId:
        type: string   
      poId:
        type: string   
      description: 
        type: string
      documentType: 
        type: string
      documentCategoryId: 
        type: string
      referenceNumber: 
        type : string
      version: 
        type: string
      location: 
        type : string
      fileName: 
        type: string
      author: 
        type : string
      pages: 
        type : number  
      keyword: 
        type: string
      documentTitle: 
        type: string
      modifiedBy:
        type: string  
  docsindividual:
    type: object
    properties:
      documentId:
        type: string
  deleteDocument:
    type: object
    properties:
      documentId:
        type: string
  docstype:      
    type: object
    properties:
      companyId:
        type: string
      typeName:
        type: string       
  getEstimateByAlphabet:
    type: object
    properties:
      companyId:
        type: string            
      estimateNumber:
        type: string         
  getclient:
    type: object
    properties:
      companyId:
        type: string            
      name:
        type: string
  addNewTimer:
    type: object
    properties:
      companyId:
        type: string
      companyEmployeeId:
        type: string
      projectId:
        type: string     
      contactId:
        type: string            
      projectItemId:
        type: string
      startDate:
        type: string            
      endDate:
        type: string
      startTime:
        type: string            
      endTime:
        type: string
      totalHours:
        type: string            
      description:
        type: string
      wageRate:
        type: string            
      employeeApproved:
        type: string
      createdBy:
        type: string            
      hoursDt:
        type: string
      hoursRt:
        type: string            
      hoursOt:
        type: string
  updateTimerInfo:
    type: object
    properties:
      timerId:
        type: string     
      contactId:
        type: string            
      companyId:
        type: string            
      companyEmployeeId:
        type: string            
      projectItemId:
        type: string
      startDate:
        type: string            
      endDate:
        type: string
      startTime:
        type: string            
      endTime:
        type: string
      totalHours:
        type: string            
      description:
        type: string
      wageRate:
        type: string            
      employeeApproved:
        type: string
      modifiedBy:
        type: string            
      hoursDt:
        type: string
      hoursRt:
        type: string            
      hoursOt:
        type: string                                                
  getTimerDetails:
    type: object
    properties:
      companyId:
        type: string            
      timerId:
        type: string
  getTimersList:
    type: object
    properties:
      companyId:
        type: string
  deleteTimerInfo:
    type: object
    properties:
      timerId:
        type: string        
  addNewTailGate:
    type: object
    properties:
      companyId:
        type: string            
      dateEffective:
        type: string
      dateExpire:
        type: string     
      topic:
        type: string            
      content:
        type: string
  updateTailGateInfo:
    type: object
    properties:
      tailgatesId:
        type: string            
      dateEffective:
        type: string
      dateExpire:
        type: string            
      topic:
        type: string
      content:
        type: string                                                
  getTailGateDetails:
    type: object
    properties:
      companyId:
        type: string            
      tailgatesId:
        type: string
  getTailGateList:
    type: object
    properties:
      companyId:
        type: string                                       
  addFaxDetails:
    type: object
    properties:
      companyId:
        type: string
      subject:
        type: string  
      tempalte:
        type: string 
      description:
        type: string 
      from:
        type: string
      categoryId:
        type: string
      createdDate:
        type: string
      pages:
        type: number                     
  addCallDetails:
    type: object
    properties:
      companyId:
        type: string
      subject:
        type: string
      description:
        type: string 
      categoryId:
        type: string
      from:
        type: string
      startTime:
        type: string
      endTime:
        type: string 
      date:
        type: string
      createDate:
        type: string
      duration:
        type: number
      result:
        type: string 
      createdBy:
        type: string                            
  addLetterDetails:
    type: object
    properties:
      companyId:
        type: string
      subject:
        type: string
      description:
        type: string
      categoryId:
        type: string
      createdDate:
        type: string
      createdTime:
        type: string
      createdBy:
        type: string
      template:
        type: string               
  addTaskDetails:
    type: object
    properties:
      companyId:
        type: string
      priority:
        type: string
      description:
        type: string         
      subject:
        type: string  
      startdate:                            
        type: string
      startTime:
        type: string  
      duedate:
        type: string
      categoryId:
        type: string
      assignedTo:
        type: string
      assignedBy:
        type: string
      reminderDate:
        type: string
      reminderTime:
        type: string
      isComplete:
        type: boolean                                     
  addEventDetails:
    type: object
    properties:
      companyId:
        type: string
      eventName:
        type: string
      categoryId:
        type: string
      eventNotes:
        type: string
      startDate:
        type: string
      endDate:
        type: string
      startTime:
        type: string
      endTime:
        type: string
      isAllDay:
        type: boolean
      isComplete:
        type: boolean
      isCalendarEvent:
        type: boolean
      createdBy:
        type: string
      createdOn:
        type: string
      subject:
        type: string                               
  addNoteDetails:
    type: object
    properties:
      companyId:
        type: string         
      subject:
        type: string  
      categoryId:
        type: string    
      createdDate:
        type: string
      createdTime:
        type: string
      createdBy:
        type: string      
      description:
        type: string
  addEmailDetails:
    type: object
    properties:
      companyId:
        type: string
      from:
        type: string
      to:
        type: string
      cc:
        type: string
      bcc:
        type: string        
      subject:
        type: string  
      attachment:
        type: string 
      description:
        type: string        
      tempalte:
        type: string
      sendAsEmail:
        type: boolean
      sendAsHtml:
        type: boolean        
  addProject:
    type: object
    properties:
      companyId:
        type: string        
      individualId:
        type: string
      customerId:
        type: string
      title:
        type: string
      discription:
        type: string
      categoryId:
        type: string
      priorityId:
        type: string
      stageId:
        type: number
      startDate:
        type: string
      endDate:
        type: string
      percentComplete:
        type: number
      departmentId:
        type: string
      projectRate:
        type: number
  getProjectDetail:
    type: object
    properties:
      projectId:
        type: string
  updateProjectDetail:
    type: object
    properties:
      projectId:
        type: string        
      individualId:
        type: string
      customerId:
        type: string
      title:
        type: string
      discription:
        type: string
      categoryId:
        type: string
      priorityId:
        type: string
      stageId:
        type: number
      startDate:
        type: string
      endDate:
        type: string
      percentComplete:
        type: number
      departmentId:
        type: string
      projectRate:
        type: number
  deleteProject:
    type: object
    properties:
      contactId:
        type: string
  addProjectCategory:
    type: object
    properties:
      companyId:
        type: string
      noteDetailId:
        type: string
      userId:
        type: string  
      categoryName:
        type: string
  getProjectDropdownList:
    type: object
    properties: 
      projectId:
        type: string
      companyId:
        type: string
  noteDetails:
    type: object
    properties: 
      companyId:
        type: string
      noteDetailId:
        type: string 
  eventDetails:
    type: object
    properties: 
      companyId:
        type: string
      eventDetailId:
        type: string
  taskdetails:
    type: object
    properties: 
      companyId:
        type: string
      taskDetailId:
        type: string
  emaildetails:
    type: object
    properties: 
      companyId:
        type: string
      emailDetailId:
        type: string
  faxdetails:
    type: object
    properties: 
      companyId:
        type: string
      faxDetailId:
        type: string
  calldetails:
    type: object
    properties: 
      companyId:
        type: string
      callDetailId:
        type: string
  letterdetails:
    type: object
    properties: 
      companyId:
        type: string
      letterDetailId:
        type: string                                                                
  activityDetails:
    type: object
    properties: 
      activityId:
        type: string
      activityType:
        type: number
  addCompanyEmailTemplate:
    type: object
    properties: 
      subject:
        type: string
      title:
        type: string
      description:
        type: string
      code:
        type: string 
  getCompanyEmailTemplateDetails:
    type: object
    properties: 
      emailtemplateId:
        type: string              
  updateCompanyEmailTemplate:
    type: object
    properties: 
      emailtemplateId:
        type: string
      subject:
        type: string
      title:
        type: string
      description:
        type: string
      code:
        type: string
  deleteactivity:
    type: object
    properties: 
      activityId:
        type: string
  addExpenses:
    type: object
    properties:
      companyId:
        type: string
      amount:
        type: number  
      enteredOn:
        type: string
      createdBy:
        type: string
      projectId:
        type: string
      type:
        type: string
      description:
        type: string
      purchasedAt:
        type: string
      ccLast:
        type: string
      note:
        type: string
      estimateImage:
        type: string               
  updateExpenses:
    type: object
    properties:
      expenseId:
        type: string
      companyId:
        type: string
      amount:
        type: number   
      enteredOn:
        type: string
      createdBy:
        type: string
      projectId:
        type: string
      type:
        type: string
      description:
        type: string
      purchasedAt:
        type: string
      ccLast:
        type: string
      notes:
        type: string
      estimateImage:
        type: string
  expensesId:
    type: object
    properties: 
      expenseId:
        type: string
  getDetailByProject:
    type: object
    properties: 
      companyId:
        type: string
      projectId:
        type: string
  addProjecttask:
    type: object
    properties: 
      companyId:
        type: string
      projectId:
        type: string
      itemId:
        type: string
      itemName:
        type: string
      description:
        type: string
      start:
        type: string
      end:
        type: string
      duration:
        type: number
      dailyAverage:
        type: number
  getProjecttask:
    type: object
    properties: 
      projectId:
        type: string
  getEventsList:
    type: object
    properties: 
      companyId:
        type: string
  getEventsListByContact:
    type: object
    properties: 
      companyId:
        type: string        
  addActivityCategory:
    type: object
    properties: 
      companyId:
        type: string
      userId:
        type: string
      categoryName:
        type: string
      activityType:
        type: string        
  getCompanyEmailTemplatesById:
    type: object
    properties: 
      templateId:
        type: string
  addPurchaseOrder:
    type: object
    properties:  
      companyId:
        type: string
      contactId:
        type: string
      customerId:
        type: string
      projectId:
        type: string
      vendor:
        type: string
      vendorAddress1:
        type: string
      vendorAddress2:
        type: string
      vendorState:
        type: string
      vendorCity:
        type: string
      vendorZip:
        type: string
      shippingAddress1:
        type: string
      shippingAddress2:
        type: string
      shippingState:
        type: string
      shippingCity:
        type: string
      shippingZip:
        type: string
      requestedBy:
        type: string
      approvedBy:
        type: string
      shipVia:
        type: string
      statusId:
        type: number
      approvalStatusId:
        type: number
      createdDate:
        type: string
      approvedDate:
        type: string
      uponReceipt:
        type: string
      title:
        type: string
      trackingNumber:
        type: string
      shipDate:
        type: string
      notes:
        type: string
  deletePO:
    type: object
    properties:  
      poId:
        type: string
  deletePOAttach:
    type: object
    properties:  
      attachmentId:
        type: string
  getPOlist:
    type: object
    properties:  
      companyId:
        type: string
  updatePurchaseOrder:
    type: object
    properties:  
      companyId:
        type: string
      poId:
        type: string
      vendor:
        type: string
      vendorAddress1:
        type: string
      vendorAddress2:
        type: string
      vendorState:
        type: string
      vendorCity:
        type: string
      vendorZip:
        type: string
      shippingAddress1:
        type: string
      shippingAddress2:
        type: string
      shippingState:
        type: string
      shippingCity:
        type: string
      shippingZip:
        type: string
      requestedBy:
        type: string
      approvedBy:
        type: string
      shipVia:
        type: string
      trackingNumber:
        type: string
      statusId:
        type: number
      approvalStatusId:
        type: number
      createdDate:
        type: string
      approvedDate:
        type: string
      uponReceipt:
        type: string
      title:
        type: string
      shipDate:
        type: string
      notes:
        type: string
  addDailyReport:
    type: object
    properties: 
      projectId:
        type: string
      companyId:
        type: string
      customerId:
        type: string     
      problems:
        type: string
      date:
        type: string
      projectNumber:
        type: string
      daysOfWeek:
        type: string
      projectManager:
        type: string
      leadTech:
        type: string        
      attention:
        type: string
      projectTitle:
        type: string
      dailyProduction:
        type: string
      resolution:
        type: string
      workPlan:
        type: string
      onSiteTeamMember:
        type: string
      notes:
        type: string
      preparedBy:
        type: string
  getDailyReportList:
    type: object
    properties: 
      projectId:
        type: string
      companyId:
        type: string
  getDailyReportDetails:
    type: object
    properties: 
      projectId:
        type: string
      dailyReportId:
        type: string        
  newDailyReport:
    type: object
    properties: 
      projectId:
        type: string        
  getProjecttaskByAlphabet:
    type: object
    properties: 
      projectId:
        type: string
      itemName:
        type: string
  getTools:
    type: object
    properties: 
      toolsId:
        type: string
  updateTools:
    type: object
    properties: 
      toolsId:
        type: string
      itemId:
        type: string
  dailyReportSendToCustomer:
    type: object
    properties: 
      dailyReportId:
        type: string
      projectId:
        type: string 
      attentionName:
        type: string 
      customerName:
        type: string 
      LoggedInUser:
        type: string
  deleteDailyReport:
    type: object
    properties: 
      dailyReportId:
        type: string
  getServiceOrderlist:
    type: object
    properties:  
      companyId:
        type: string
  deleteServiceOrder:
    type: object
    properties: 
      orderId:
        type: string
  getServiceDetail:
    type: object
    properties: 
      orderId:
        type: string
  getFilterOpportunitiesList:
    type: object
    properties: 
      filterType:
        type: string
      startDate:
        type: string
      endDate:
        type: string
      companyId:
        type: string
  addServiceOrder:
    type: object
    properties:  
      companyId:
        type: string
      customerId:
        type: string
      title:
        type: string
      billingAddress1:
        type: string
      billingAddress2:
        type: string
      billingState:
        type: string
      billingCity:
        type: string
      billingZip:
        type: string
      shippingAddress1:
        type: string
      shippingAddress2:
        type: string
      shippingState:
        type: string
      shippingCity:
        type: string
      shippingZip:
        type: string
      descriptionWork:
        type: string
      poNumber:
        type: string
      statusId:
        type: number
      stageId:
        type: number
      salesRep:
        type: string
      class:
        type: number
      technicianSignature:
        type: string
      customerSignature:
        type: string
      techSignDate:
        type: string
      custSignDate:
        type: string
      subtotal:
        type: number
      tax:
        type: number
      total:
        type: number
      memo:
        type: string
  updateServiceOrder:
    type: object
    properties:  
      orderId:
        type: string
      customerId:
        type: string
      title:
        type: string
      billingAddress1:
        type: string
      billingAddress2:
        type: string
      billingState:
        type: string
      billingCity:
        type: string
      billingZip:
        type: string
      shippingAddress1:
        type: string
      shippingAddress2:
        type: string
      shippingState:
        type: string
      shippingCity:
        type: string
      shippingZip:
        type: string
      descriptionWork:
        type: string
      poNumber:
        type: string
      statusId:
        type: number
      stageId:
        type: number
      salesRep:
        type: string
      class:
        type: number
      technicianSignature:
        type: string
      customerSignature:
        type: string
      techSignDate:
        type: string
      custSignDate:
        type: string
      subtotal:
        type: number
      tax:
        type: number
      total:
        type: number
      memo:
        type: string
  getActivityCategoryList:
    type: object
    properties: 
      activityType:
        type: string
      companyId:
        type: string
  addInvoice:
    type: object
    properties:  
      contactId:
        type: string
      projectId:
        type: string
      individualId:
        type: string
      opportunityId:
        type: string
      orderId:
        type: string
      estimateId:
        type: string
      poNumber:
        type: string
      title:
        type: string
      createdDate:
        type: string
      dueDate:
        type: string
      invoiceNumber:
        type: string
      invoiceItem:
        type: string
      terms:
        type: number
      class:
        type: number
      salesRep:
        type: string
      customerMessage:
        type: number
      memo:
        type: string
      total:
        type: number
      paymentsApplied:
        type: string
      balanceDue:
        type: number
      laborCost:
        type: number
      equipmentCost:
        type: number
      materialCost:
        type: number           
      stageId:
        type: string
      taxRate:
        type: number
      billingAddress1:
        type: string
      billingAddress2:
        type: string
      billingCity:
        type: string
      billingState:
        type: string
      billingZip:
        type: number
      shippingAddress1:
        type: string
      shippingAddress2:
        type: string
      shippingCity:
        type: string
      shippingState:
        type: string
      shippingZip:
        type: number           
      otherCost:
        type: number     
      createdBy:
        type: string
  updateInvoice:
    type: object
    properties:
      invoiceId:
        type: string  
      contactId:
        type: string
      projectId:
        type: string
      individualId:
        type: string
      opportunityId:
        type: string
      orderId:
        type: string
      estimateId:
        type: string
      poNumber:
        type: string
      title:
        type: string
      createdDate:
        type: string
      dueDate:
        type: string
      invoiceNumber:
        type: string
      invoiceItem:
        type: string
      terms:
        type: number
      class:
        type: number
      salesRep:
        type: string
      customerMessage:
        type: number
      memo:
        type: string
      total:
        type: number
      paymentsApplied:
        type: string
      balanceDue:
        type: number
      laborCost:
        type: number
      equipmentCost:
        type: number
      materialCost:
        type: number           
      stageId:
        type: string
      taxRate:
        type: number
      billingAddress1:
        type: string
      billingAddress2:
        type: string
      billingCity:
        type: string
      billingState:
        type: string
      billingZip:
        type: number
      shippingAddress1:
        type: string
      shippingAddress2:
        type: string
      shippingCity:
        type: string
      shippingState:
        type: string
      shippingZip:
        type: number           
      otherCost:
        type: number     
      modifiedBy:
        type: string
  deleteInvoice:
    type: object
    properties:
      invoiceId:
        type: string
  getInvoiceDetails:
    type: object
    properties:
      invoiceId:
        type: string
  getExpensesList:
    type: object
    properties:
      companyId:
        type: string                
  getIndustryList:
    type: object
    properties:
      companyId:
        type: string                
      moduleType:
        type: number
  getlaborRate:
    type: object
    properties:
      companyId:
        type: string
  addlaborRate:
    type: object
    properties:
      companyId:
        type: string                
      laborType:
        type: string
      displayName:
        type: string
      rate:
        type: number
  countForCompany:
    type: object
    properties:
      contactId:
        type: string
  geProposalList:
    type: object
    properties:
      companyId:
        type: string
  getCompanyContactList:
    type: object
    properties:
      companyId:
        type: string
      companyContactId:
        type: string
  getAssociateContactList:
    type: object
    properties:
      companyId:
        type: string
      companyContactId:
        type: string
      userType:
        type: number
  addItemRevision:
    type: object
    properties:
      companyId:
        type: string
      estimateId:
        type: string
  getRevisionList:
    type: object
    properties:
      estimateId:
        type: string
  getRevisionDetail:
    type: object
    properties:
      revisionId:
        type: string
  deleteItemRevision:
    type: object
    properties:
      revisionItemId:
        type: string
  addMemo:
    type: object
    properties:
      userName:
        type: string
      message:
        type: string
  exportEstimateCsv:
    type: object
    properties:
      revisionId:
        type: string
  addOrderType:
    type: object
    properties:
      companyId:
        type: string
      orderTypeName:
        type: string
  getOrderType:
    type: object
    properties:
      companyId:
        type: string
  getManufacturerByName:
    type: object
    properties:
      companyId:
        type: string
      manufacturer:
        type: string
  getAllUsers:
    type: object
    properties:
      companyId:
        type: string 
  getUserById:
    type: object
    properties:
      companyUserId:
        type: string
  addSignatureServiceOrder:
    type: object
    properties:
      userId:
        type: string
      orderId:
        type: string  
  getRole:
    type: object
    properties:
      companyId:
        type: string
  updateUserDetail:
    type: object
    properties:
      userId:
        type: string                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             
      firstname:
        type: string                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             
      lastname:
        type: string                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             
      name:
        type: string                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            
  removeUserPic:
    type: object
    properties:
      userId:
        type: string                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             
      path:
        type: string                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          
  removeContactPic:
    type: object
    properties:
      contactId:
        type: string                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             
      path:
        type: string                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          
  removeItemPic:
    type: object
    properties:
      itemId:
        type: string                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             
      path:
        type: string                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          
  checkToken:
    type: object
    properties:
      bearerToken:
        type: string  
  addSignatureSalesrep:
    type: object
    properties:
      contactId:
        type: string
      image :
        type: string
  getItemConfiglist:
    type: object
    properties:
      estimateId:
        type: string
  generateEstimatePdf:
    type: object
    properties:
      estimateId:
        type: string
      htmlContent:
        type: string
  addEstimateFormat:
    type: object
    properties:
      estimateId:
        type: string
  updateEstimateFormat:
    type: object
    properties:
      configId:
        type: string
  addOpportunityTag:
    type: object
    properties:
      namevalue:
        type: string
  updateItemRevisionName:
    type: object
    properties:
      revisionId:
        type: string
      revName:
        type: string
  getEstimateRevisionCount:
    type: object
    properties:
      estimateId:
        type: string
  updateEstimateItemName:
    type: object
    properties:
      itemId:
        type: string
  getFileList:
    type: object
    properties:
      opportunityId:
        type: string
  renameFileAttachment:
    type: object
    properties:
      attachmentId:
        type: string
      filename:
        type: string
  deleteFileAttachment:
    type: object
    properties:
      attachmentId:
        type: string
  restoreFileAttachment:
    type: object
    properties:
      attachmentId:
        type: string